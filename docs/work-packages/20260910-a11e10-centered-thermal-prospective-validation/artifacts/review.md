# Review

Status: GO — no unresolved P0/P1

The exact 20-station/32-fresh-burn grid completed. All source, dependency,
station, control, loading, state, candidate, score, and output identities are
bound. Preflight, loadings, evidence, and decision replayed byte-identically;
confirmation remained sealed.

The overall component passes, but the prospectively frozen requirement that
all four cohorts pass does not. Cohorts 2 and 3 show material instability in
annual lag-one and/or low-frequency error. The resulting rejection is faithful
to the contract and is not relaxed after outcome access.

GO to descriptive closure with `CENTERED_THERMAL_COMPONENT_REJECTED`. No
transfer, selector, confirmation, production, or default change is authorized.
