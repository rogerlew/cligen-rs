# Changelog-History Section for the RANDN Literature Review

Status: `EXECUTED-REVIEW-PENDING`
Date: 2026-07-10
Evidence mode: documentary — every claim cites `cligen.f` header
lines/entries (`[source]`) or the already-vendored Meyer et al. 2008
quotes (`[read]`); no new executions.

## Objective

Operator-directed ("document the changelog story"): fold the
2001–2004 C. R. Meyer changelog chronology — the RNG-replacement
attempts, the chi-square → K-S transition, the Excel/spreadsheet
derivation of the P(50)/0.8276 target, the fossils (`chitst`,
`rand_nrs` unit 73), and the criterion-circularity reading — into
[the RANDN literature review](../../lit-reviews/legacy-randn-generator-review.md)
as a new §2 subsection ("The record of the search"), preceding §3.
Existing section numbering is unchanged (cross-references from prior
dispositions stay valid).

## Review cycle

Codex dispatched (MCP, read-only) to review the new section for:
A. factual accuracy against the `cligen.f` header and code fossils
(version numbers, dates, quotes, line references); B. quote fidelity
(verbatim vs paraphrase honesty); C. coherence with the review's
existing layers/§4 content (no contradiction, no duplication);
D. soundness of the three "readings" (especially the
criterion-circularity argument); E. register/evidence-tag discipline.
Findings verbatim → `artifacts/review-codex.md`; dispositions →
`artifacts/disposition-claude.md`; close on disposition.
