# Execution plan

1. Authenticate the exact R14 abort receipt, zero-attempt state, published
   source commit, and prepared asset manifest. Verify the science asset hashes
   against R14 before creating fresh execution state.
2. Publish R14R1 to `main`. Prepare from the exact R14 asset tree; recreate
   `controller-admissions/` fresh and empty, overlay only the corrected
   capacity contract and published R14R1 wrappers, and deterministically
   generalize arbitrary-sized same-wave admission ordering.
3. Build fresh R14R1 authority, budget, run, plan, and admission identities
   under the unchanged 995 GPU-minute ceiling and xlarge-evidence profile.
   Complete the inherited calendar/missingness preflight before allocation.
4. Materialize and authenticate control admission from a verified private
   state, submit the 30-minute control, and require its validated pass before
   opening any candidate admission.
5. Materialize the four candidate admissions in their frozen order. Submit
   each role as soon as every earlier same-wave role has an authenticated
   ready-for-science setup receipt. This serializes bootstrap while allowing
   all four science phases to run concurrently.
6. Observe, pack, and collect under the unchanged 256 MB/64 MB evidence
   contract. Preserve raw streams and checkpoints until authenticated replay
   completes.
7. Authenticate collection, plan, source, allowlist, and 188-metric selector
   identities. Replay the unchanged R14 selector twice and require byte-
   identical results before cleanup.
8. Record the scientific disposition, clean exact remote and job-local state,
   release unused recovery reserve, and close the toolkit run.

No authority creation or HPC submission occurs during scaffolding.
