# A10M5R8R1 — Authority Source-Identity Remedy

Status: `EXECUTED-HOLD-CALENDAR-MISSINGNESS`
Date: 2026-07-19
Evidence mode: Mixed
Starting branch and push target: current clean `main`, push `main`

## Objective

Execute the unchanged A10M5R8 climate-statistics control/treatment experiment
under a correct full-Git-object package authority after the original private
genesis recorded a nonexistent manually expanded source SHA.

## Scope

Included: exact A10M5R8 specification, contract, sources, P1 control/treatment,
statistic registry, thresholds, firewall, one single-L40 attempt, evidence,
accounting, and cleanup. Excluded: every scientific or resource broadening,
including retries, new weights, another seed, architecture changes, solar
radiation, protected roles, and promotion.

## Authority

The operator's instruction to scaffold and execute the next work package
authorizes ordinary bounded corrective follow-through. The original authority
cannot be repaired or reset and spent no resources. R1 therefore receives an
independent development-only package authority with the unchanged 65-minute
ceiling: one 60-minute L40 primary and one five-minute exact-node cleanup
reserve.

The full source identity is taken directly from `git rev-parse HEAD` after this
corrective package is committed and pushed. A local preflight requires the
same full value in Git, asset manifest, authority input, initialized authority,
and plan before any remote operation.

## Plan

1. Publish this corrective record and its package-specific authority wrapper.
2. Rebuild immutable assets with the exact full source SHA and fail closed on
   any five-way identity disagreement.
3. Execute the unchanged A10M5R8 toolkit lifecycle and scientific experiment.
4. Reconcile evidence, resources, cleanup, roadmap/catalog, and ExecPlan.

## Execution & dispatch

Codex executes from `/Users/roger/src/cligen-rs`, starting from current
`origin/main` and pushing only `main`. Private R1 state lives beneath
`/Users/roger/.cache/cligen-rs/a10m5r8r1-climate-objective/`.

## Gates

All A10M5R8 scientific, firewall, operational, and repository gates are
inherited unchanged. R1 additionally requires exact full-source-SHA agreement
before `doctor` and records the original zero-spend ledger as preserved.

## Exit criteria

Scientific success and hold outcomes are exactly A10M5R8's. Any source,
authority, resource, protected-role, or cleanup discrepancy is a separate
fail-closed hold. No R1 outcome edits A10M5R8 history.

## Artifacts

- `artifacts/build_control_records.py` — package-specific wrapper around the
  frozen A10M5R8 builder
- live evidence, decision, toolkit/resource records, review, and gates

## Disposition

Job `1014023` reconstructed the accepted control exactly and passed the
synthetic dispersion test, then held before treatment training because the
implementation required eight fully observed Gregorian years. A10M1 marks
leap days `source_observed=false`, leaving 10,950 accepted rows on the
10,958-day 1980–2009 axis and therefore zero all-day-complete windows. The job
failed after 246 seconds, used five charged GPU-minutes, opened no protected
roles, cleaned job-local state, and closed with the durable root absent.

Corrective A10M5R8R2 preserves the date axis and excludes only accepted
missing core rows from aggregates and the core proper-score guard. It does not
impute leap days or change the eight-year estimand, statistic registry,
architecture, seed, weights, thresholds, or role firewall.
