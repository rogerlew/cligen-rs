# A5b Post-Climate WEPP Amendment v1

Date: 2026-07-13
Status: **FROZEN BEFORE WEPP RESPONSE OUTPUT**

## Boundary

The accepted candidate climate matrix already existed when this amendment was
required. Its manifest SHA-256 was
`b52d20b6e472995491ae3d81433f54a709a2a53c65c8b4753957c0ecb0193b50`,
with `candidate_cli_bytes_removed_after_wepp: false`. The climate evidence and
the pre-candidate freeze remain immutable.

The first downstream v4 invocation completed both accepted-input verifiers,
built the exact pinned WEPP executable, and then failed closed before
publishing any response or execution record. No WEPP archive, campaign index,
analysis output, candidate-CLI lifecycle transition, or quarantine survived.
The reported coordinate was `ca042319`, `faithful_off`, 30 years, replicate 3
(legacy burn 503). No candidate response or candidate comparison statistic was
observed. The only inspected response values were the zero runoff/peak values
needed to classify this faithful-off format branch.

Because the pre-candidate freeze binds the v4 runner and campaign, they remain
byte-for-byte independent. The correction lives in
`run-wepp-matrix-v5.py`, `wepp-campaign-v2.md`, and `analyze-wepp-v5.py`.
Candidate climate evidence is not relabeled or regenerated; downstream WEPP
evidence must bind the v5 files explicitly.

## Source-built reproduction

The failed coordinate was regenerated byte-for-byte from the accepted A5a
record with CLI SHA-256
`e1b0a25f7c09125ca2ac295b5ceb9e6d0b0a34e06598370d200036cfa641c416`
and 779,050 bytes. It was executed with pinned WEPP executable SHA-256
`dccb55f3980e287ada5541b7801f9b9fa79b4b1d65addb97d6914317bc4a4527`.

The 4,050-byte abbreviated event stream had SHA-256
`5819b40d07863382b22a07f254f806d8327ddadb5f25bae8157670cd01fedd39`.
After the common first 48 preamble lines, line 49 was the unique
`ANNUAL AVERAGE SUMMARIES` marker. The annual summary reported 490 storms and
zero rain-storm runoff events. WEPP had correctly omitted the full seven-line
hillslope/event-section suffix and every event block.

The 138,613-byte companion element stream had SHA-256
`2480929b4bdb86ceebde23f4084ab955a82afd35868fe6146d51f15c87791fda`.
It contained exactly 720 parsed records and 720 unique mapped keys spanning all
30 years; every `Runoff` and `PeakRO` value was exactly zero. Standard output
had SHA-256
`3426f6f9ac0b0f0b1d781cae488197a7612c0a1253447347337929200623fe41`,
standard error was empty with SHA-256
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`,
and WEPP exited successfully. These raw diagnostic streams remain untracked
and are removed after the amendment is sealed.

## v5 rule

Extractor `a5b_wepp_p326_response_extractor_v5` admits a zero-record event
stream only when the annual-summary marker occurs at the exact source-built
line-49 seam. The stationary preamble, station-line exception, unique header,
unique pinned version, and unique annual marker remain mandatory. A nonzero
event stream retains the complete v4 55-line preamble and block grammar.

The zero-record event result is not accepted alone. The companion element
parser must still validate all rows, mapped years, numeric formats, output
roles, and raw-stream bindings, and every reduced runoff and peak value must be
exactly zero. A displaced marker, an empty full event section, any event block
following the zero-record seam, any `PeakRO` overflow, or any nonzero companion
runoff/peak is fatal.

## Verification

The v5 self-test builds the exact pinned WEPP executable and retains every v4
positive and adversarial vector. It additionally accepts the exact zero-record
seam, rejects a displaced seam and an empty full event section, accepts a
complete zero companion element surface, and rejects a nonzero companion peak.
The retained real diagnostic coordinate parses to zero event records, 720
element records, zero annual runoff sum, and zero annual peak maximum.

Both the v5 runner and independent v5 analyzer self-tests passed before the
hash inventory was sealed. Static undefined-name checks passed for both
sources. The full production invocation must still repeat the accepted A5a and
A5b evidence verifiers before executing any WEPP job.
