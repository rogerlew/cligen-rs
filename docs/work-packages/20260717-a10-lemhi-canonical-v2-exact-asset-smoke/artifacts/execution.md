# Execution

Published source commit `37a2127` and a fresh immutable 20-minute authority
bound the exact candidate artifacts. The candidate-level firewall passed
before remote mutation. All eleven stage receipts passed bytes, hashes,
promotion, remote revalidation, and declared executable intent; notably the
Cargo-vendor receipt carried the candidate's frozen `13d7f4...` hash.

Job `1013866` started immediately on idle `node03`, ran alone on one L40, and
completed in 236 seconds. Lemhi again supplied `LD_LIBRARY_PATH` at entry. The
wrapper recorded only its presence, cleared prohibited state, reconstructed
the exact environment, and passed all 27 gates. The exact candidate-level
firewall passed again before attestation.

The evidence archive was promoted and sanitized; job-local and durable roots
were proven absent. The primary consumed 15 requested GPU-minutes, the unused
5-minute recovery reserve was released, and no recovery allocation ran. The
immutable attestation binds candidate, plan, job, evidence, cleanup, terminal,
and exact frozen asset identities.

Slurm exposed exact elapsed seconds but the tested controller revision left
`actual_gpu_minutes` null in its job receipt. This did not obscure scheduler
settlement or requested-budget enforcement; exact `ElapsedRaw=236` is retained
in scheduler evidence. The post-run toolkit correction now records exact
allocated GPU-seconds and a ceiling-rounded integer GPU-minute ledger value.
