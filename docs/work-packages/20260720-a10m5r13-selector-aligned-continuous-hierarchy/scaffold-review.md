# Scaffold review and disposition

Disposition: `ACCEPT-SCAFFOLD-FOR-PUBLICATION-AND-EXECUTION`

The two arms now differ only in how the first of four inherited slow OU factors
is decoded. Both retain the same innovations and common parameters. The base
learns four flexible loadings; the shared arm constrains that factor to a
rank-one scalar loading times a four-head field vector and retains three other
flexible slow factors. Both begin with the dedicated factor's output at zero.

The annual objective is checkpoint-active and replaces the two inherited
annual blocks with the exact four equal-weight R12 selector families. The
published R12 temporal generation, observation, metric, scoring, solar, and
confirmation firewalls remain unchanged.

No scientific result exists yet. The next authorized action after publication
is toolkit execution under the plan and official large-evidence profile.

An independent final review reran the full pinned verifier and accepted the
scaffold after confirming the exact staged candidate self-test, all six annual
lag/dependence paths, member-wise rather than pooled dependence, full parent
asset authentication, collection/plan and data-root replay binding, and exact
six-site predecessor PRISM provenance. The frozen predecessor replay pin has
SHA-256
`b6a2119eed0d042052d2d606628c4698d9937cce029896f0a5785c54a11c9670`.

The first local materialization attempt then failed closed before copying or
allocating because R12R1 had executed `temporal_select.py` from its published
package instead of staging it in the remote asset tree. The bounded correction
removes that false parent-asset assumption and stages the exact published
R12R1 selector bytes under their already-frozen SHA-256. A regression test
preserves the actual predecessor roster distinction.
