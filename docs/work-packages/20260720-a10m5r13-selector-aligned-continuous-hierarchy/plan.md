# Execution plan

1. Verify the inheritance manifest, contracts, calendar fixtures, selector
   identity, and all package tests from published `main`.
2. Materialize a package-local portfolio contract from the exact R12R1 parent
   plus the frozen 16-year/objective overlay. Stage the inherited control,
   comparator, calendar, runtime, and selector assets by exact SHA-256.
3. Create fresh authority with a 515 GPU-minute ceiling and the published
   `lemhi-v2-large-evidence.json` profile (SHA-256 `fba24ee...0b45`). The
   expected 96 MB stream evidence remains below its 128 MB expanded and 64 MB
   per-file caps. Complete calendar preflight before any reservation.
4. Materialize admission and submit the 30-minute control. Observe all control,
   setup, calendar, environment, and cleanup gates.
5. Materialize admission for the base candidate, submit it, and wait for its
   authenticated setup-ready receipt. Bind that receipt in admission for the
   shared-state candidate and submit it. Run both 240-minute L40 roles
   concurrently; each trains seeds 147031, 271828, and 314159 serially.
6. Observe both roles, pack the exact allowlist with the large-evidence profile,
   collect raw `.npz` streams and `.pt` checkpoints byte-exactly, project text
   evidence, and do not clean before selector replay is authenticated.
7. Run package-local `artifacts/run_temporal_replay.py` before cleanup. It
   authenticates published source, collection, allowlist identities, and both
   stream archives, then replays the unchanged R12 selector twice in isolated
   roots and requires byte-identical results and sealed protected roles.
8. Record disposition only from the authenticated replay receipt, then perform
   exact remote and job-local cleanup and close.

The scaffold itself does not submit a GPU job. Once this exact source is
published, the operator's authorization applies immediately to the listed
toolkit sequence; no additional scientific-design handoff is required.
