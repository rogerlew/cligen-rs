# A10M5R4R2R1 execution disposition

Terminal: `HOLD-A10-EVALUATION-YEAR-AXIS`

- six of six reconstruction/generation roles passed at attempt zero;
- all 54 per-role operational/scientific gates passed;
- 288 registered 100-year neural streams were collected;
- Slurm jobs 1014010--1014015 used 1,238 GPU-seconds and 24 rounded
  GPU-minutes;
- R2 plus R2R1 used 27 rounded GPU-minutes against the aggregate 185-minute
  campaign bound;
- recovery was not invoked;
- development-selection and confirmation remained sealed;
- toolkit raw collection, evidence projection, exact job-local cleanup,
  marker-bound durable cleanup, and close all passed; and
- the local six-site by two-comparator by eight-burn matrix completed.

No `evaluation.json` score was published. The first bootstrap replicate failed
while assigning the 30 sampled observation blocks synthetic labels: the
formula `source_year + 400 * (position + 1)` exceeded year 9999. This is an
evaluation implementation defect, not a climate-generation or evidence
failure. The exact 354-file, 280,551,300-byte comparator tree is retained
privately under SHA-256
`c4d7bf9c2b8441b89ceda42ec1ef5e7976ee533f998eff473bece2f967154607`.
