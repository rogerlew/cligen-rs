# A10M5R4R2R1 — Reconstruction Identity Remedy

Status: `SCAFFOLDED`
Date: 2026-07-18
Evidence mode: Prospective corrective freeze; no R2R1 generated-output access
Starting branch and push target: `main`, push `main`

## Objective

Resume A10M5R4R2's unchanged realized temporal-dispersion hypothesis after
replacing its empirically invalid TorchScript-container reconstruction gate
with exact accepted checkpoint-payload and semantic training-state identities.

## Scope

The six sites, observation role and window, P1/P2 pair, three seeds, stream
members, horizon, comparator arms, metrics, uncertainty procedure, thresholds,
role firewall, and decision rules remain exactly those frozen by A10M5R4R2.
This package changes only model-reconstruction identity and the wrapper's
failure-evidence serialization. It does not reopen model fitting, selection,
confirmation, or any scientific threshold.

## Authority

A10M5R4R2 established that the accepted checkpoint payload, model record,
training cursor, and validation metrics reproduce exactly while a newly
serialized TorchScript ZIP archive does not. The six accepted semantic
identities are frozen in `artifacts/reconstruction-contract.json` from
A10M5R3R1's authenticated evidence. The original accepted export hashes remain
recorded as provenance but are explicitly non-gating.

Live allocation requires a revision-2 authority that names the published
R2R1 source commit and preserves the remaining campaign budget. The existing
R2 authority names only source commit `f704820...`; it cannot legally derive
this corrective source commit or change its immutable package identity.

## Plan

1. Verify the R2 hold, exact cleanup, unchanged temporal contract, and all six
   accepted semantic identities.
2. Reconstruct each model once in the canonical Lemhi environment and require
   exact checkpoint payload, checkpoint cursor, model record, and validation
   identities before loading the newly serialized CPU export.
3. Generate the unchanged registered streams and collect honest success or
   failure receipts for every role.
4. Run the already-frozen observation, faithful, and stochastic-PRISM arms;
   score the unchanged matrix and issue the retained-capacity decision.
5. Perform exact cleanup, repository gates, roadmap/catalog reconciliation,
   and authorize A10M5R5 only if the unchanged temporal gate passes.

## Gates

- `artifacts/verify_freeze.py` emits `A10M5R4R2R1-FREEZE-READY`;
- all six checkpoint payload and semantic identities reproduce exactly;
- TorchScript archive hashes are recorded but never used as reconstruction
  acceptance gates;
- every success and pre-stream failure produces a collectable nonempty gate
  receipt and an explicit stream-presence/absence record;
- the R2 temporal contract and role firewalls remain unchanged; and
- `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and
  `cargo test` pass.

## Exit criteria

Execution ends with the unchanged typed temporal decision, or an evidence-
specific hold unrelated to the retired container-byte identity. Allocation
must not begin until the published R2R1 commit is covered by valid authority.

## Artifacts

- `artifacts/reconstruction-contract.json` — exact six-model semantic gate;
- `artifacts/design-freeze.md` — corrective boundary and empirical basis;
- `artifacts/jobs/` — corrected reconstruction/generation and asset wrappers;
- `artifacts/verify_freeze.py` — fail-closed prospective verifier.
