# Execution plan

1. Verify exact R13 inheritance, the canonical Daymet calendar/missingness
   preflight, the 2x2 architecture freeze, the 188-row objective coverage
   report, and all package tests from published `main`.
2. Prepare a fresh R14 asset tree by byte-verifying the R13R1 execution assets,
   retaining valid comparator/corpus/runtime/control/selector assets, and
   overlaying published R14 contracts, aligned objective, continuous core,
   four role wrappers, authority builder, and admission controller.
3. Create fresh authority at a 995 GPU-minute ceiling under the xlarge-evidence
   profile. Calendar preflight must pass before any resource reservation.
4. Materialize admission and submit the 30-minute control. Observe setup,
   calendar, environment, science, physical-support, firewall, and cleanup
   gates.
5. After the authenticated setup-ready receipt, materialize four independent
   admissions and submit arms A--D concurrently. Each trains the three frozen
   seeds serially inside one 240-minute L40 job.
6. Observe all roles once. Apply stop rules without scientific retries; use
   the five-minute recovery reserve only for ambiguous job-local cleanup.
7. Collect the exact allowlist under the xlarge-evidence profile. Preserve raw
   stream/checkpoint evidence outside Git and do not clean before replay.
8. Replay the unchanged selector twice from authenticated evidence, require
   byte-identical outputs, verify protected roles stayed sealed, and record
   the R14 terminal only from that receipt.
9. Clean remote and job-local state, close the toolkit run, publish bounded
   evidence/disposition/review, and reconcile roadmap, catalog, and ExecPlan.
