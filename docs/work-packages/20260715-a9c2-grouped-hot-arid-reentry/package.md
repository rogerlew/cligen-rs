# A9c2 — Grouped Hot-Arid Re-entry and Observed Comparison

Status: `SCAFFOLDED`
Date: 2026-07-15
Evidence mode: Mixed
Scaffolding authorization: operator direction on 2026-07-15 from committed
`main` at `3122afc81b3fa0790d19974b6803f56a81c1b2ae`

## Objective

Replace A9c's uncalibrated per-station storm-event availability floors with a
prospectively frozen, station-balanced grouped evaluation, expand the hot-arid
development roster from two to at least five metadata-selected locations, and
rerun the complete two-class observed-development comparison under a new
campaign identity. A9c2 must determine support from candidate-blind precision
and power behavior at the actual grouped design without touching the locked A9
confirmation series.

## Scope

Included:

- exact verification of the committed A9a, A9b, and revised A9c records;
- a metadata-only inventory and immutable selection rule for all eligible
  hot-arid USCRN development locations;
- retention of both A9c hot-arid sites plus enough eligible additions to reach
  at least five distinct locations;
- a prospectively versioned registry for all four hot-arid storm-descriptor
  fit and evaluation rules, replacing per-station count availability with
  station-balanced group estimators and site-heterogeneity guards;
- candidate-blind calibration of grouped precision, false-failure behavior,
  and power before candidate development scoring;
- a new role/campaign identity, complete refitting of both candidate classes,
  complete numeric null recalibration, and the full staged A9 comparison;
- a public scientific report, consolidated review, verification, and exactly
  one registered A9c2 terminal.

Excluded:

- changing A9c's evidence, failed-cell arithmetic, or terminal;
- reusing A9c's five partial fits or null thresholds as A9c2 results;
- reading, summarizing, previewing, quality-querying, or replacing any locked
  A9a confirmation station-year series;
- selecting or dropping development stations based on event counts observed
  after the roster rule is frozen;
- a production Rust profile, station schema, runtime classifier, fallback,
  realized-output repair, openWEPP, or WEPPcloud integration;
- A9d confirmation or A9e runtime work.

## Authority

- [A9c revision-2 disposition](../20260715-a9c-observed-development/artifacts/post-acceptance-operator-disposition.md)
  defines the correction and immutable A9c boundary.
- [A9c re-entry requirements](../20260715-a9c-observed-development/artifacts/reentry-requirements.md)
  define the minimum successor obligations.
- [SPEC-A9](../../specifications/SPEC-A9-RESEARCH-FOUNDATION.md), the A9a
  family envelope, data plan, objective registry, and locked confirmation
  roster remain authoritative except for the explicit prospective grouped-
  availability amendment frozen by A9c2.
- A9b's schemas, firewall, deterministic harness, Philox ownership, candidate
  plugin boundary, and selector remain authoritative and immutable.
- ADR-0001 protects faithful generation; ADR-0002 and ADR-0004 govern quality
  adjudication and any later promotion.

The package-local predecessor manifest binds exact authorities and the
operator disposition. The context-complete design contract records all
scientific and access boundaries needed for execution without relying on
conversation memory.

## Plan

1. Verify predecessor hashes, A9b fixture replay, A9c report revision 2, A9c
   terminal arithmetic, and confirmation metadata-only state.
2. Using metadata only, inventory every eligible USCRN hot-arid site, apply
   the frozen climate/spatial/source rule, and publish the complete accepted
   and rejected roster without station-series access.
3. Before series access, publish a SPEC-A9 grouped-evaluation amendment and
   complete versioned objective registry without editing SPEC-A9 revision 1
   or A9a's registry. Freeze at least five hot-arid locations, retaining Yuma
   and Stovepipe Wells, plus exact role periods, source/QC normalization, fit
   hierarchy, grouping, equal-station weighting, seasonal/zero-event rules,
   leave-one-site-out diagnostics, resampling, spatial-dependence treatment,
   candidate identities, resources, and amendment rules.
4. If separately authorized by the frozen role contract, perform a support-
   only acquisition for the entire accepted roster; retain every site and
   calibrate the grouped estimator's precision and power without candidate
   inputs or confirmation access.
5. Freeze numeric grouped-availability and familywise null rules. Hold before
   fitting if the grouped design cannot be calibrated; never repair the result
   by deleting a dry site or adding an outcome-selected wet site.
6. Create a new campaign identity, refit every registered configuration of
   both actual candidate classes, rerun structural/recovery/monthly checks,
   and recalibrate all seven families at both horizons.
7. Execute short screening, full development, and Pareto replay across all six
   strata; publish the complete 31-objective vector, group and site-
   heterogeneity diagnostics, availability, uncertainty, and selection trace.
8. Freeze at most one complete A9d candidate, author the report, complete
   consolidated review, run repository/package gates, and close with exactly
   one registered terminal.

## Execution & dispatch

Only scaffolding is authorized. No A9c2 station metadata inventory beyond
already committed records, station-year access, support query, fitting,
simulation, candidate evaluation, confirmation access, A9d freeze, commit, or
push is authorized by this package.

When the operator separately dispatches execution, the kickoff must name
`/Users/roger/src/cligen-rs`, start from the then-current clean `origin/main`,
use branch `main`, and push only to `main`. Before any station-series access,
execution must replace the scaffold marker with exact source commit and role/
campaign freeze hashes. A side branch is not an authorized package record.

## Gates

- exact predecessor hashes and terminals reproduce;
- A9c remains `HOLD-A9C-GATE-CALIBRATION` and every A9c object is exposed;
- the 18-site confirmation roster remains metadata-only with zero target-
  series access;
- the metadata rule selects every eligible hot-arid development site and at
  least five distinct locations without outcome-based substitution;
- development sites remain station-disjoint from confirmation and pass the
  prospectively frozen partition-separation rule;
- observed and generated group statistics give each station equal total
  weight, regardless of event count;
- leave-one-site-out and maximum-site diagnostics prevent a pooled pass from
  hiding one location's structural failure;
- support is calibrated candidate-blind at the actual site/year/event design;
  the former 150/200 per-station rules are not reused;
- the grouped design reaches at least 0.80 power for newly justified,
  predeclared candidate-neutral material perturbations and the pre-frozen
  precision criterion;
- fit-side descriptor identifiability and convergence rules are versioned so
  the former 50/150/1,000 count rules cannot silently reinstate the old design;
- SPEC-A9 revision 1 and the A9a objective registry remain byte-immutable;
  the amendment and A9c2 registry have independent IDs and hashes;
- all accepted sites remain in analysis after support-only access;
- both candidate classes pass structural, fit, recovery, reconciliation, and
  applicability gates under the new campaign identity;
- 500 paired null identities per family/horizon are newly generated and
  frozen before candidate development scoring;
- all 31 objectives and site/stratum/group/horizon availability are published;
- the exact selector reproduces and freezes no more than one A9d candidate;
- report review finishes with zero open P1/P2 findings;
- `git diff --check` plus an untracked authored-file whitespace scan;
- `cargo fmt --check`;
- `cargo clippy --all-targets -- -D warnings`; and
- `cargo test`.

Coverage/CRAP is not triggered unless execution changes production functions
under `crates/`; such a change is outside A9c2 scope.

## Exit criteria

Success is exactly `CANDIDATE-FROZEN-READY-A9D` with one complete sealed
candidate and no confirmation-series access. Registered holds are:

- `HOLD-A9C2-PREDECESSOR` — an exact predecessor or revision-2 authority does
  not reproduce;
- `HOLD-A9C2-DESIGN-FREEZE` — the versioned objective, fit, group, power, or
  spatial-dependence contract cannot be ratified before series access;
- `HOLD-A9C2-HOT-ARID-ROSTER` — fewer than five eligible, confirmation-safe
  hot-arid development locations satisfy the frozen metadata rule;
- `HOLD-A9C2-GROUP-POWER` — candidate-blind precision/power calibration
  cannot support the grouped design;
- `HOLD-A9C2-DATA-ROLE` — authorized roles cannot be materialized without
  overlap, source failure, or confirmation exposure;
- `HOLD-A9C2-MODEL-CLASS-EQUIVALENCE` — actual implementations are
  structurally isomorphic or collapse into the excluded intersection;
- `HOLD-A9C2-FIT-APPLICABILITY` — either class cannot fit mandatory strata;
- `HOLD-A9C2-MONTHLY-RECONCILIATION` — analytic/quadrature budgets fail;
- `HOLD-A9C2-NO-SELECTABLE-CANDIDATE` — the frozen comparison rejects both
  classes; or
- `HOLD-A9C2-RESOURCE-BOUND` — complete evidence cannot be produced under the
  pre-access ceiling.

Every hold retains negative evidence and authorizes neither roster repair nor
A9d/A9e continuation. The operator-selected identifier is A9c2; it does not
imply that an A9c1 package exists or is required.

## Artifacts

- `artifacts/context-and-design-contract.md` — context-complete scientific,
  grouping, access, reuse, and execution contract.
- `artifacts/predecessor-manifest-v1.json` — exact committed authority hashes.
- `artifacts/execution-kickoff.md` — branch-complete future dispatch template;
  not an execution authorization.
- `artifacts/verify-scaffold.py` and `gate-results.md` — exact predecessor,
  boundary, report, LFS, and repository validation.
- `artifacts/README.md` — artifact inventory and expected execution outputs.
