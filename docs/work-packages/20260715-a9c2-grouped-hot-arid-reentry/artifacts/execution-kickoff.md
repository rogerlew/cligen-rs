# A9c2 future execution kickoff

Status: template only; not dispatched

From the repository root, execute
`docs/work-packages/20260715-a9c2-grouped-hot-arid-reentry/` from the then-
current clean `origin/main`, using branch `main` and pushing only to `main`.

Read `AGENTS.md`, the package, every package-local artifact, and every exact
file in `predecessor-manifest-v1.json` before acting. Verify all predecessor
hashes. Do not rely on conversation memory.

Execute the phases in package order. Freeze the complete metadata roster,
partition-separation rule, role periods, group formulas, equal-station
weights, heterogeneity guards, resampling, support-calibration method,
candidate grid, resource bounds, and amendment policy before any new station-
year access. Access no locked confirmation series. Stop at the first registered
hold; do not replace a site, weaken a rule, or inspect candidate rankings to
rescue the campaign.

This prompt becomes active only when the operator explicitly dispatches A9c2
execution. Scaffolding, committing, or pushing this template is not dispatch.
