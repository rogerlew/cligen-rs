# Closeout review and disposition

Disposition: `ACCEPT-EXECUTED-HOLD`

R13R2 repaired the exact replay-authentication boundary without changing the
R13 science, selector, evidence, or confirmation firewall. Attempt 1's
pre-selector parser failure is retained rather than hidden. Attempt 2 proves
the correction against the same byte-pinned R13R1 collection and emits a
record-authenticated replay identity before cleanup.

The retained artifacts are appropriately bounded: exact temporal result and
replay identity, compact execution summary, and the two close receipts. Large
candidate streams, checkpoints, observation shards, raw scheduler logs, and
the full collected evidence tree remain outside Git. The copied JSON artifacts
are byte-identical to their cache/publication sources.

Scientifically, both continuous candidates fail the inherited temporal gate
by a wide margin and neither is eligible for promotion. The continuous
hierarchy's tiny non-gating advantage does not justify selecting it. Stable
learned medium and slow time scales show that avoiding monthly state resets
was implemented and learned; the HOLD therefore attributes the remaining gap
to candidate adequacy rather than monthly quantization or replay mechanics.

The successor should retire the rank-one arm and retain the flexible OU core.
A concurrent 2x2 crosses a smooth uncentered day-of-year/geography climatology
head with centered OU access to distribution scale heads. This directly tests
the two structural limitations exposed by the selector while keeping the
latent stochastic process daily and continuous and solar sealed.

Protected roles were never opened. Cleanup occurred only after authenticated
replay, proved remote and job-local absence, and the toolkit run closed. The
package is complete with an honest scientific HOLD.
