# A10M5R7R2 — Authorized Architecture Execution

Status: `SCAFFOLDED`
Date: 2026-07-18
Evidence mode: Mixed
Starting branch and push target: clean `main` at `2e00700`, push `main`

## Objective

Execute the prospectively frozen A10M5R7 structural architecture diagnostic
under the operator-authorized fresh package-scoped resource authority, then
carry out its deterministic architecture selection and conditional unchanged
temporal adjudication.

## Authority

The operator's 2026-07-18 continuation explicitly authorizes this new package
after A10M5R7R1 established that the prior hash-chained authority was exhausted
by conservative reservation accounting. R2 receives a new authority and
resource-budget identifier capped at 57 GPU-minutes: one single-attempt L40
primary of at most 52 minutes and one exact-node recovery reserve of at most
five minutes.

This authorization does not change the scientific contract. R2 uses the exact
corrected A10M5R7 source, accepted P1 checkpoint identities, six sites,
candidate-fit-only normalizers, three inference modes, five component groups,
diagnostic thresholds, decision order, temporal score, bootstrap, comparator
tree, and protected-role firewall already frozen in A10M5R7/R1.

## Plan

1. Reverify the corrected source, six observation-shard identities, canonical
   runtime/corpus assets, and prospective diagnostic contract.
2. Initialize exactly one new development-only authority, complete the Lemhi
   v2 lifecycle, and submit one single-attempt 52-minute L40 job.
3. Collect the complete diagnostic, replay the deterministic architecture
   decision, and run the unchanged local temporal adjudication only if the
   generated-feedback candidate advances.
4. Reconcile accounting, recovery non-invocation or exact recovery, durable
   cleanup, roadmap/catalog state, review, and repository gates.

## Gates and exits

All scientific and operational gates and terminal outcomes are inherited
unchanged from
`20260718-a10m5r7-structural-architecture-identification`. R2 additionally
requires its new authority to remain within 57 GPU-minutes and binds the
operator authorization, source commit, asset manifest, plan, scheduler job,
accounting, collection, and cleanup into one package record.

No R2 exit opens N3/elevation, spatial, development-selection, confirmation,
public runtime, or production work automatically.
