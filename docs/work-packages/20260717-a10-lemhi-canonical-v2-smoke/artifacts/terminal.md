# Terminal

`HOLD-A10-CANONICAL-V2-SMOKE-ENVIRONMENT-CLOSURE`

The candidate did not fail a CUDA, Python, Rust, model, or performance test;
those gates were never reached. The operational environment-entry contract
failed closed because `--export=NONE` did not itself guarantee absence of all
prohibited ambient Python/loader variables.

A successor must receive a new package identity. It may record only typed
presence flags or hashes for ambient inputs, clear them, reconstruct the exact
registered environment, and assert closure before the first Python import. It
must bind the unchanged candidate semantic SHA-256. This package cannot be
retried, cannot emit an attestation, cannot designate canonical v2, and cannot
authorize A10M5.
