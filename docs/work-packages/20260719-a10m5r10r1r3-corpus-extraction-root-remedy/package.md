# A10M5R10R1R3 — Corpus Extraction Root Remedy

Status: `SCAFFOLDED`
Date: 2026-07-19
Evidence mode: Mixed
Starting branch and push target: current `main`, push `main`

## Objective

Execute a fresh, coherent rerun of the complete A10M5R10 architecture
portfolio without changing its science, calendar, role matrix, portable
bootstrap, admission policy, or job-local-capacity contract. This package
corrects the A10M5R10R1R2 science-entry failure by restoring the proven R0
corpus extraction root in both control and candidate wrappers.

## Predecessor evidence and frozen science

A10M5R10R1R2 authenticated source, assets, authority, plan, staged remote
tree, login admission, portable Python 3.11.15, environment installation,
setup-payload deletion, and job-local cleanup. Control job `1014054` then
failed at science entry because `corpus.tar` contains a top-level `corpus/`
prefix but the wrapper extracted it into `$job_local/corpus`. The effective
data root became `$job_local/corpus/corpus` while the frozen science opened
`$job_local/corpus/artifacts`. No control row or candidate result was
published, and no candidate was admitted.

This package binds the exact committed R1R2 HOLD disposition, operational
summary, cleanup record, and resource ledger, plus the earlier R1R1 and R1
operational HOLDs and independently reviewed A10M5O1R2 toolkit hardening. The
bindings are frozen in `artifacts/predecessor-evidence-identities.json`.
Prior run evidence cannot substitute for a successor role.

The twelve direct and transitive science dependencies remain byte-identical to
the original A10M5R10 portfolio. The complete 1,440-object calendar and
missingness preflight remains byte-identical: revision-2 Daymet transform,
normalized axis, observed masks, leap-year fixtures, window boundaries, and
core/physics counts are pinned before any resource reservation.

## Frozen corpus remedy

The canonical `corpus.tar` is frozen at 224,040,960 bytes and SHA-256
`8770e127f8413eedd47d50670c359e450988444a8c4d8d43ca5645619a1b0a17`.
It contains exactly 101 safe relative members under the sole top-level prefix
`corpus/`: 98 accepted objects and three corpus manifests. A pre-authority
layout verifier requires that identity, member count, prefix, safe paths, and
the three required manifests.

The only behavioral correction in staged science-entry wrappers is made in
exactly two files, `run_control.sh` and `run_candidate.sh`:

```sh
tar -xf "$run_root/corpus.tar" -C "$job_local"
```

Both wrappers continue to create and pass `$job_local/corpus`. Extracting the
archive into its parent therefore exposes the frozen manifests at
`$job_local/corpus/artifacts`, exactly as the successful R0 contract did. No
strip-components rule, fallback path, selector, or corpus inference is added.

Portable runtime extraction still occurs with POSIX shell before compute-side
Python. Setup diagnostics and inline checks still use the portable Python
3.11 runtime. The outer terminal-only finalizer remains Python-3.6-compatible,
and admission remains closed after any observed exhausted candidate failure.

The control role completes first. Candidate roles then run in the unchanged
five ordered waves of two: monthly residual, annual/monthly residual,
hierarchical joint-factor, climate-normal hierarchical state-space, and
physics-conditioned hierarchical adapter, each at K1 and K2. At most two
candidate jobs are live and at most one bootstraps.

## Authority and resource bound

The package permits one 30-minute one-L40 control predecessor, ten 90-minute
single-attempt one-L40 candidate roles, and one five-minute exact-node cleanup
reserve: a 935 GPU-minute ceiling. It defines fresh package, run, authority,
budget, and scheduler-token identities. There are no retries, arrays,
multi-rank jobs, substitutions, or changes to the frozen objective, data roles,
model families, capacities, seeds, selector, or terminal rules.

## Gates

- exact R1R2, R1R1, R1, and A10M5O1R2 files match the frozen predecessor
  manifest and committed identities;
- the twelve science dependencies and calendar profile match before and after
  materialization;
- calendar preflight SHA-256 is
  `7f58a20eae635d9453ea86a473072b9e2597195ffc1d71660184d3e70b130a68`;
- corpus archive bytes, SHA-256, 101-member count, sole `corpus/` prefix, safe
  relative paths, and three required manifests pass before authority;
- a real miniature archive regression proves the R1R2 destination nests the
  corpus and the corrected destination exposes `corpus/artifacts`;
- both science-entry wrappers require extraction into `$job_local` and reject
  extraction into `$job_local/corpus`;
- after normalizing package terminal labels, the only behavioral R1R2-to-R1R3
  wrapper difference is the two extraction-destination lines;
- all portable-bootstrap, terminal-finalizer, admission, failure-closure, and
  log-bound tests remain passing;
- the exact ten roles remain partitioned into five ordered two-role waves;
- one attempt per role and the 935 GPU-minute ceiling remain frozen;
- every submitted role publishes setup, supervisor, Slurm, cleanup, and gate
  evidence before aggregation or authenticated whole-matrix stop;
- the unchanged A10M5R10 selector replays only after all ten successor results
  pass; and
- package tests, shell syntax, `git diff --check`, `cargo fmt --check`,
  `cargo clippy --all-targets -- -D warnings`, and `cargo test` pass.

Coverage/CRAP is not triggered because this package does not change production
functions under `crates/`.

## Exit criteria

Scientific terminals remain exactly those frozen by A10M5R10:
`A10M5R10-PORTFOLIO-READY`, `HOLD-A10M5R10-SINGLE-CANDIDATE`,
`HOLD-A10M5R10-NO-CANDIDATE`, or an exact calendar, identity, role, support,
evidence, resource, or cleanup hold. An operational failure records an exact
`HOLD-A10M5R10R1R3-*` condition without interpreting candidate science.

## Artifacts

- `artifacts/job-local-capacity-contract.json` — unchanged resource, portable
  bootstrap, admission, and wave policy plus the corpus-root correction;
- `artifacts/corpus-layout-pin.json` and
  `artifacts/verify_corpus_layout.py` — exact archive identity and safe layout
  pre-authority gate, with the pin itself frozen at 859 bytes and SHA-256
  `30058e8cbe2b420f899bcc67b5d90042e39c921ba1e75ea107d81c624437b51e`;
- `artifacts/predecessor-evidence-identities.json` — exact predecessor HOLD and
  toolkit-hardening identities;
- `artifacts/science-dependency-identities.json` and
  `artifacts/verify_science_identity.py` — unchanged science freeze;
- `artifacts/calendar-preflight.json` — complete calendar/missingness preflight;
- `artifacts/admission-protocol.md` — login admission, portable compute
  bootstrap, corrected corpus extraction, and stop-matrix procedure;
- `artifacts/verify_freeze.py` — science, predecessor, corpus, interpreter,
  execution-policy, and two-file-delta verifier;
- `artifacts/scaffold-review.md` — independent review findings, remediation,
  and final `ACCEPT` disposition;
- `artifacts/test_admission_checker.py` and
  `artifacts/test_operational_identity.py` — retained and corrective regression
  tests; and
- `artifacts/jobs/` — immutable asset preparation, control records, staged
  admission, portable bootstrap, durable diagnostics, wrappers, and frozen
  science sources.
