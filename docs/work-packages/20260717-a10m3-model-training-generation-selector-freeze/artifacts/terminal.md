# Terminal

Terminal: `A10M3-DESIGN-FROZEN`

All model, training, checkpoint, generation, benchmark, objective,
applicability, selector, and finite resource interfaces are frozen before the
first A10 neural fit or output. The canonical Lemhi ID/hash, accepted A10M1 v2
corpus identities, B0/B1 identities, strictly positive 0.10 improvement
margins, noninferiority guards, exact runtime boundaries, missingness behavior,
and selection tie breaks are machine-readable and executable.

No GPU allocation, candidate fit/output, development target series, or
confirmation target was accessed. The package authorizes only the bounded
A10M4 qualification described in `a10m4-handoff.md`.
