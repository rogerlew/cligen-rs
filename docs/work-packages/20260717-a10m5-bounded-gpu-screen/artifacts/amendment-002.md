# Prospective amendment 002 — sequential local-capacity schedule and finalizer

The first two r2 allocations (`1013870`, `1013871`) settled before training.
Concurrent expansion of two canonical environments exhausted node03 `/tmp`;
the accepted supervisor nevertheless removed both exact job-local trees. The
generated wrapper's controller finalizer also contained a newline-escaping
error, so its failure receipt was reconstructed from Slurm and durable
supervisor evidence before collection.

The r2 lineage requested 240 GPU-minutes and consumed 332 GPU-seconds. Its
staged root was removed by the exact original marker after collection. A
toolkit limitation prevented formal `close`: narrowing unstarted roles through
an authorized plan amendment changed the current plan hash without updating
the immutable remote owner marker, so normal `clean` could not authenticate
the amended hash. Private state and evidence are retained at `COLLECTED`; no
remote root or job-local target remains.

The wrapper generator now emits a literal escaped newline and its generated
Python finalizer is syntax-checked locally. The successor schedule submits one
configuration at a time. This is within the frozen maximum concurrency of two
and avoids changing any scientific or per-job resource contract. No A10M5
training, validation, development, or confirmation output existed before this
amendment.
