# A10M5 terminal

Terminal: `HOLD-A10-NO-VALID-NEURAL-FIT`

All twelve frozen configurations completed, but every isolated one-core CPU
export exceeded the unchanged 2 GiB RSS safeguard. Observed peaks were
3.09--3.13 GiB (3,317,673,984--3,363,221,504 bytes). All runtime ratios were
`PASS` below 5x, all exports were below 1 MiB, and all cold loads were about
1.20--1.21 seconds. These strengths do not override a hard safeguard.

Promotion count is zero in both pooling classes. A10M6 is not authorized and
no development or confirmation target may be opened. A10M5R1 is the bounded
successor to attribute and remedy the canonical CPU-export memory floor without
relaxing the threshold or refitting against protected roles.

Toolkit terminal: `LEMHI-TOOLKIT-RUN-CLOSED`; durable remote root absent and
all job-local roots verified absent.
