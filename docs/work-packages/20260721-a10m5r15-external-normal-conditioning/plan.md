# Execution plan

1. From published `main`, authenticate ADR-0006/0007, specification revision 1,
   the R14R2R2 terminal, R14 arm-B/D evidence, and the PRISM runtime/source
   bundle identities in `artifacts/predecessor-pin.json`.
2. Implement the exact four-arm source surface frozen in
   `artifacts/science-contract.json`. Reuse R14 code by hash where unchanged;
   introduce a separately named normals loader and replacement-family module.
   No generated output is permitted in this phase.
3. Add fixture tests proving containing-cell lookup, units/layer order,
   candidate-fit-only normalization, E0/E2C normals exclusion, matched-pair
   initialization, continuous-time no-reset behavior, structural member
   centering, parameter counts, and CPU export timing boundaries.
4. Prepare a fresh asset tree from the authenticated R14R2R2 execution source.
   Overlay only the published A10M5R15 contracts, job sources, four wrappers,
   asset-preflight code, authority builder, admission materializer, and replay.
5. Materialize fresh toolkit authority with a 515 L40-minute-equivalent ceiling.
   Before any reservation, query and freeze all 1,440 fit/validation coordinates
   plus six temporal sites from the exact PRISM runtime bundle. Publish zero
   missing/duplicate/non-finite cells, candidate-fit-only normalizer statistics,
   exact hashes, and the canonical Daymet calendar receipt.
6. Submit the 30-minute control. It reconstructs P2 controls, authenticates the
   portable CPU path, confirms protected roles remain sealed, and publishes the
   setup-ready receipt. A failed control ends before candidate submission.
7. Immediately materialize portfolio admission from a fresh node snapshot and
   submit one two-L40 allocation. Execute E0/E1 concurrently in wave 0, wait and
   reap both, then execute E2C/E2 concurrently in wave 1. Each child trains the
   three frozen seeds serially with disjoint output/cache roots.
8. Observe once. Do not retry scientific work. Use the five-minute recovery
   allowance only to reconcile ambiguous cleanup state.
9. Collect the exact allowlist, retain raw streams/checkpoints outside Git, and
   run two independent authenticated selector passes. Require byte identity and
   compute the two matched calibrated attribution contrasts separately from
   temporal eligibility.
10. Run the unchanged normative CPU benchmark for all four exact bundles.
    Immutable bundle loading, coordinate extraction, normalization, and station
    conditioning occur before the warm timer without model/RNG advance; E1/E2
    per-day mapping evaluation remains timed. Apply ADR-0006 boundaries.
11. Clean remote and job-local state, close the toolkit run, publish bounded
    evidence and one terminal, disposition independent-review findings, and
    reconcile the roadmap, package catalog, specification status, and ExecPlan.
