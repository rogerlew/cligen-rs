# A11E independent review and disposition

Date: 2026-08-25

Reviewer: delegated subagent `a11_scaffold_review`

Final disposition: **PASS — no unresolved P0 or P1 findings.** The scientific
implementation is completion-ready after routine evidence reconciliation and
source publication.

## Material findings resolved

- Bound each revision-1 strategy ID to exact core-only capabilities, stages,
  evaluator, metric-set, and uncertainty identities in schema and runtime.
- Integrated covariance reconciliation, location forcing, and the daily core
  instead of advertising disconnected primitives.
- Replaced misleading finite-ensemble covariance conditioning with fixed
  population-law transforms, preserving prefixes, persistence, and
  fields-greater-than-years execution.
- Retired the approximate empirical-copula arm before execution and registered
  an exact latent Gaussian scalar-AR(1) arm. The empirical comparison remains
  the fixed-length circular block bootstrap.
- Renamed the block strategy so it is not confused with the standard
  geometric-length stationary bootstrap.
- Enforced fit role/region, 30-year site cohorts, non-Boolean year horizons,
  Philox keys and domains, distinct core streams, malformed support, and full
  forcing preflight before any draw.
- Rejected asymmetric covariance and diagonal drift; recorded the zero-annual-
  sensitivity tie rule; distinguished population targets from realized sample
  diagnostics.
- Strengthened evidence for persistence and cross-variable moments, complete
  circular blocks, prefix consistency, 48 fields over 30 years for both arms,
  exact daily invariants, texture, replay, and non-consuming failure paths.

## Non-blocking dispositions

- The pinned runtime has no JSON Schema implementation. Tests compare the
  schema's exact revision-1 identity constants with the runtime and exercise
  canonical/mutated manifests through the strict runtime validator. Full
  Draft 2020-12 execution is deferred until a validator becomes a registered
  dependency; this package does not claim general schema-engine conformance.
- Revision 1 intentionally exposes continuous annual/monthly states on a
  modeling scale. It does not claim a typed physical-scale precipitation,
  range, or integer adapter. Those additions require new registered strategy
  or adapter identities.
- No observed-data execution occurred. Calendar, missingness, corpus-role, and
  published-source preflight remain mandatory in the later execution package.

No confirmation, production, faithful-mode, or public-interface breach was
found.

