# A10M5R1 R2 attribution disposition

R2 Slurm job 1013929 failed after 101 seconds because the published shell
loop did not invoke the newly frozen `jit-current-nondeterministic` worker.
Python correctly refused to aggregate the absent record. The supervisor
proved job-local cleanup; toolkit observation recorded a failed role with
2 actual GPU-minutes and no scientific inference.

The shell enumeration is corrected prospectively. If candidate acceptance
does not itself establish or reject the deterministic child recipe, the
package's fourth and final diagnostic allocation may run the corrected matrix
under a new run identifier. This source error does not change any threshold,
identity, model, or protected-data boundary.
