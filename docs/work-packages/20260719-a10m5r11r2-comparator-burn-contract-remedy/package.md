# A10M5R11R2 — Comparator Burn Contract Remedy

Status: `SCAFFOLDED`
Date: 2026-07-19
Evidence mode: Retrospective scoring of immutable authenticated development evidence
Starting branch and push target: current `main`, push `main`

## Objective

Complete the ratified A10 retained-adapter temporal adjudication by restoring
the omitted eight-member comparator burn list to the local evaluation contract
and scoring the immutable A10M5R11R1 streams without another GPU run.

## Predecessor and bounded correction

A10M5R11R1 passed admission, all four distributed jobs, every candidate
science gate, authenticated collection, resource settlement, and exact
cleanup. It produced 432 support-valid candidate streams for the three frozen
architectures. The first local scorer invocation failed before comparator
generation because the transcribed temporal contract lacked the already
ratified `generation.stochastic_burn_counts` field consumed by the unchanged
scorer.

This package adds exactly
`[101,1101,2101,3101,4101,5101,6101,7101]`. It changes no candidate,
observation, site, metric, scale, bootstrap, threshold, calendar transform,
or decision rule. The scorer and metric implementations are reused byte for
byte from committed A10M5R11R1. No model is reconstructed or run, and the GPU
allocation is exactly zero.

## Execution sequence

1. Verify the committed R1 source, three stream identities, toolkit closure,
   scorer, metrics, sites, binary, and restored burn list.
2. Generate faithful and stochastic localized-PRISM comparator arms locally
   for the six frozen sites and eight frozen burns.
3. Run the leap-safe 1,000-replicate paired bootstrap and emit the typed
   temporal decision.
4. Repeat the complete local comparator/scorer operation in a fresh scratch
   tree and require byte-identical results.
5. Review the evidence, reconcile package/catalog/roadmap/ExecPlan state, and
   preserve confirmation and solar firewalls.

## Exit criteria

The scientific terminal is `A10M5R11-TEMPORAL-READY` when at least one
configuration meets both inherited noninferiority limits, otherwise
`HOLD-A10M5R11-NO-TEMPORALLY-ELIGIBLE-CANDIDATE`. Any evidence, identity,
replay, or local execution failure receives an exact `HOLD-A10M5R11R2-*`
terminal and no architecture interpretation.

## Downstream solar boundary

Solar remains unopened. A later solar package must use a procedural
latitude/day-of-year astronomical or clear-sky envelope and learn a stochastic
clearness/cloud residual coupled only to generated precipitation and
temperature. It must not consume observed daily weather during generation.
