# A10M5R15R2R4 Disposition

Status: `EXECUTED-HOLD-OCCUPANCY-PARSER`

The control materialization completed successfully as Slurm job `1060885` in
19 L40-minutes. Its authenticated receipt is retained in
`artifacts/execution-r0-control-job.json`.

The portfolio was not submitted. R2R4's frozen occupancy parser rejected a
valid `%b=N/A` Slurm row before admission; this is an operational parser gap,
not scientific evidence. Confirmation evidence remains untouched. The fresh
R2R5 successor retains the frozen study and corrects admission through
scheduler-pending allocation plus a job-start GPU-count recheck.
