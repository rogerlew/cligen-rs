# A10M5R13R1 — Admission Controller Materialization Remedy

Status: `EXECUTED-HOLD-NO-TEMPORALLY-ELIGIBLE-CANDIDATE`
Date: 2026-07-20
Evidence mode: Operational successor; identical R13 science
Starting branch and push target: current `main`, push `main`

## Objective

Execute the exact A10M5R13 selector-aligned continuous-hierarchy comparison
after its authenticated zero-allocation abort. The predecessor reached remote
verification but submitted no job: its cache-local transformed admission
controller could not prove itself to be a repository path, and the initialized
authority could not admit the later controller correction.

R13R1 changes only the controller lineage. It publishes a committed admission
wrapper, authenticates the published R13 controller before importing it, and
binds fresh R13R1 package/run/authority/budget/admission identities. Candidate
roles, architectures, science contracts, seeds, random field, selector,
calendar, comparator, thresholds, resources, evidence profile, and terminals
remain exactly R13.

## Inherited scientific freeze

- Roles remain `selector-aligned-continuous-hierarchy-k2` and
  `selector-aligned-shared-slow-climate-state-k2`.
- P2/K2, three seeds, 16-year 5,844-row/5,840-observed windows, 13 origins,
  eight generated members, and six development sites remain unchanged.
- The four equal-weight selector-scaled annual families and continuous daily
  OU evolution remain unchanged.
- Scientific terminals remain `A10M5R13-TEMPORAL-READY` and
  `HOLD-A10M5R13-NO-TEMPORALLY-ELIGIBLE-CANDIDATE`.
- Solar and protected confirmation roles remain sealed.

## Operational correction

The package/run IDs are
`20260720-a10m5r13r1-admission-controller-materialization-remedy` and
`a10m5r13r1-admission-controller-materialization-remedy-r0`. The fresh
authority and budget use the same R13R1 stem. Admission receipts use
`a10m5r13r1-submission-admission`.

Asset preparation authenticates every byte in the real R13 prepared parent,
removes all copied controller admissions, overlays only exact published R13R1
controller/builder assets, and deterministically rewrites operational package,
run, and admission identifiers. The committed admission wrapper verifies the
R13 controller against published Git bytes before import and rejects R13 or
R12 receipt types.

## Resources and execution firewall

The fresh ceiling is 515 GPU-minutes: one 30-minute control, two concurrent
240-minute L40 candidates, and five recovery minutes. One attempt per role;
no scientific retry. This scaffold does not create authority, reserve compute,
or submit HPC. After exact publication, the existing operator authorization
applies to the frozen sequence without another scientific decision.

## Exit

Authenticated two-pass replay uses the unchanged R13 selector and scientific
terminals. Cleanup and toolkit close occur only after the replay receipt.

## Execution result

The controller remedy succeeded and the complete three-role matrix ran once
under the published R13R1 authority. Control job `1016150` and concurrent
candidate jobs `1016152` and `1016153` passed every registered gate and
charged 20, 84, and 81 GPU-minutes respectively. Collection promoted all 51
allowlisted members under evidence projection v5.

R13R1's first replay command exposed a semantic-plan authentication defect
before selector execution. The bounded zero-GPU R13R2 successor authenticated
the same collected evidence, reconstructed the semantic plan, and produced
byte-identical selector passes. Neither candidate met the unchanged temporal
limits, so the scientific terminal is
`HOLD-A10M5R13-NO-TEMPORALLY-ELIGIBLE-CANDIDATE`.

After the R13R2 replay receipt, marker-bound cleanup proved both durable and
job-local state absent. The unused five-minute recovery reservation was
released and the toolkit run closed as `LEMHI-TOOLKIT-RUN-CLOSED`.

See `artifacts/execution-disposition.md`, `artifacts/execution-gates.md`, and
`artifacts/operational-summary.md` for the closeout evidence.
