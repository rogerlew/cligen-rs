# Execution plan

1. Verify exact R13 parent source at commit
   `c849cdd3e0fcf8abf496b6ff987776a08d8b30cf`, its authenticated
   zero-allocation abort receipt, byte-identical science assets, and sealed
   protected roles.
2. Publish R13R1 to `main`. Prepare from the exact real R13 asset directory;
   authenticate its complete manifest and every copied file, recreate
   `controller-admissions/` fresh and empty, and overlay only published R13R1
   controller assets.
3. Build fresh R13R1 authority and plan records with a 515 GPU-minute ceiling
   and the official `lemhi-v2-large-evidence.json` profile. Complete calendar
   preflight before allocation.
4. Before each submit, invoke the committed R13R1 admission wrapper. It must
   authenticate the inherited R13 controller before import, materialize a
   fresh `a10m5r13r1-submission-admission` receipt from current private state,
   and verify the fetched remote receipt.
5. Submit and observe the 30-minute control. Only after its authenticated pass,
   materialize both candidate admissions and submit the two 240-minute roles
   in one concurrent wave with serialized bootstrap setup.
6. Observe, pack, and collect under the published 128 MB/64 MB evidence-volume
   contract. Preserve raw streams and checkpoints until replay completes.
7. Authenticate collection/plan/source/allowlist identities and run the
   unchanged R13 selector twice before cleanup. Require byte-identical results,
   exact inherited PRISM provenance, and sealed protected roles.
8. Record the R13 scientific disposition, clean exact remote and job-local
   state, and close the toolkit run.

No authority creation or HPC submission occurs during scaffolding.
