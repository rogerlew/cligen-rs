# A9d — Successor Development and Locked Confirmation

Status: `EXECUTED-HOLD-NO-SELECTABLE-CANDIDATE`
Date: 2026-07-15
Evidence mode: Mixed
Scaffolding and execution authorization: operator dispatch on 2026-07-15
from clean `main` at `1d0350eed8549067eca41047c0eef43949822c69`,
targeting `main`

## Objective

Complete the A9 research decision in one package. Prospectively accept A9c4's
92-cell observed/faithful evidence surface with its explicit regime/family
limits, fit and test the already specified bounded event-context laws, tune
the two frozen A9 model classes through a bounded development grid, seal at
most one candidate, and—only after that seal—consume the untouched 18-site
confirmation corpus exactly once.

## Scope

Included:

- exact verification of the accepted A9a--A9c4 authorities;
- prospective acceptance of A9c4's 68 non-storm common-support cells and 24
  inherited grouped-storm cells at both horizons, with the 19 excluded cells
  retained as report-only evidence;
- the unchanged alternating-renewal and latent-regime model classes, a bounded
  18-configuration development grid, and the prospectively amended positive-
  amount mean calibration within the existing fit recipe; exact occurrence
  calibration was withdrawn before any A9d fit or candidate output because it
  was incompatible with the frozen class supports;
- event air temperature in identity/Gaussian space, solar radiation and wind
  speed in log/lognormal space, and relative humidity in logit/logit-normal
  space, with no realized-output clipping or repair;
- staged 30/100-year development evaluation against faithful CLIGEN, using
  the inherited candidate-blind thresholds and frozen lexicographic rule;
- at most one complete development candidate freeze;
- conditional acquisition and byte sealing of the exact frozen 18-site
  confirmation corpus, station-specific Daymet fits, twelve new burns, one
  atomic sealed-to-consumed transition, and one final confirmation decision;
- one public scientific report, consolidated independent review, package
  verification, roadmap/catalog closure, and repository gates.

Excluded:

- rewriting A9c4's terminal or relabeling its H1;
- adding model classes, selectors, fallback layers, runtime climate
  classifiers, station substitution, output repair, or post-confirmation
  tuning;
- treating the 19 report-only cells as favorable values;
- production Rust profiles, station schemas, openWEPP, WEPPcloud, or public-
  default changes.

## Authority

- [A9c4's accepted report](../../reports/a9c4-context-support-completeness-report.md)
  establishes the 92/19 surface and permits a prospectively identified
  successor to accept it with explicit limits.
- [A9a's model envelope](../20260715-a9a-successor-family-foundation/artifacts/model-family-envelope.md),
  [tuning contract](../20260715-a9a-successor-family-foundation/artifacts/tuning-harness-contract.md),
  and [data/evaluation plan](../20260715-a9a-successor-family-foundation/artifacts/data-and-evaluation-plan.md)
  remain authoritative except for the package-local one-package execution
  sequence and evidence-surface decision frozen here.
- The A9c3 grouped estimator, candidate-blind calibration, objective
  definitions, faithful comparator, and selection rule remain unchanged.
- ADR-0001 protects faithful generation; ADR-0002 governs extension-quality
  evidence. This research package changes no production function.

## Plan

1. Freeze dispatch, predecessor identities, the 92-cell surface, fit recipe,
   configuration grid, support transforms, stages, thresholds, burns,
   confirmation crosswalk, access lifecycle, decision rules, and resources
   before corrected candidate output.
2. Refit all 18 registered configurations on the existing coefficient-fit
   role; apply only the frozen monthly calibration and context transforms;
   reproduce monthly reconciliation, structural distinction, and support.
3. Execute short screen, full development, and eight-burn replay on the frozen
   surface; retain every attempt and seal at most one candidate under the
   unchanged no-material-degradation selector.
4. If no candidate seals, close A9d at its named final development hold. If
   one seals, acquire confirmation bytes through the custodian-only path,
   create the complete station fit and manifest, then atomically consume the
   sealed corpus once.
5. Run twelve-burn nested 30/100-year confirmation against the nearest frozen
   `us-2015` faithful parameter comparator, publish all retained and report-
   only cells, and return the registered pass or final failure without tuning.
6. Author and independently review the public report, run all gates, reconcile
   roadmap/catalog state, and commit/push the complete package.

## Execution & dispatch

The operator authorized the complete A9d sequence on 2026-07-15 from clean
`main` equal to `origin/main` at
`1d0350eed8549067eca41047c0eef43949822c69`. The package deliberately contains
both corrective development and conditional locked confirmation; it does not
create administrative successor packages between those stages. `main` is the
only push target.

## Gates

- every predecessor and source identity passes;
- the design freeze and 92-cell acceptance predate corrected A9d output;
- all 19 excluded cells remain report-only at both horizons;
- corrected solar/wind values are positive and humidity is strictly between
  zero and 100, with no clipping or repair;
- both candidate classes retain at least one valid fresh fit and remain
  structurally non-isomorphic;
- monthly calibration and numerical reconciliation pass for every retained
  fit;
- deterministic replay, nested prefix, calendar, support, provenance, and
  complete objective publication pass;
- at most one development candidate is sealed;
- confirmation target bytes remain scientifically unread unless the complete
  candidate and confirmation manifests are sealed;
- any confirmation path has one recorded atomic consume transition, twelve
  frozen burns, no target-driven tuning, and one terminal;
- the report and review have zero open P1/P2 findings;
- `git diff --check` plus authored-text whitespace scan;
- `cargo fmt --check`;
- `cargo clippy --all-targets -- -D warnings`; and
- `cargo test`.

Coverage/CRAP is not triggered because this package changes no production
function under `crates/`.

## Exit criteria

The package has exactly one final terminal:

- `CONFIRMATION-PASS-READY-A9E` — one sealed candidate passed the one-shot
  confirmation rule;
- `CONFIRMATION-FAIL-FINAL` — one sealed candidate failed confirmation and
  cannot be tuned further in this campaign;
- `HOLD-A9D-NO-SELECTABLE-CANDIDATE` — bounded development produced no
  candidate satisfying the frozen selector;
- `HOLD-A9D-PREDECESSOR-INTEGRITY`, `HOLD-A9D-DATA-ROLE`,
  `HOLD-A9D-CONTEXT-SUPPORT`, `HOLD-A9D-MODEL-CLASS-EQUIVALENCE`,
  `HOLD-A9D-FIT-APPLICABILITY`, `HOLD-A9D-MONTHLY-RECONCILIATION`, or
  `HOLD-A9D-RESOURCE-BOUND` — an exact pre-confirmation failure; or
- `HOLD-A9D-CONFIRMATION-DATA` or `HOLD-A9D-CONFIRMATION-ROLE` — a sealed
  candidate existed but the locked corpus could not be validly consumed.

No terminal implicitly authorizes production integration.

## Execution result

A9d completed its bounded development path in this package and stopped before
confirmation with `HOLD-A9D-NO-SELECTABLE-CANDIDATE`.

- The accepted A9c4 surface remained 92 retained and 19 excluded former-
  mandatory cells at both horizons.
- The pre-output amendment removed analytically incompatible exact occurrence
  calibration. All 18 configurations then produced valid fresh fits; positive-
  amount calibration, uncertainty-adjusted monthly reconciliation, and the
  two-class structural audit passed.
- The executed funnel was 18 short-screen, four full-development, and two
  eight-burn replay configurations. Every staged result was complete with no
  hard failure and no unavailable retained cell.
- Strict event-context support passed on all 720 registered short-screen
  30-year prefixes without clipping or repair. The broader 1,040-attempt
  engineering validator used inclusive physical bounds, so the all-output
  quantifier in H2 is only partially supported.
- `renewal-p010-q090` retained three material-degradation rows, all hot-arid:
  aggregate at 30 and 100 years and extreme at 100 years.
  `latent-k3-p010-q090` retained 17. The unchanged selector therefore sealed
  zero candidates.
- No confirmation target series was acquired or scientifically accessed. The
  conditional one-shot confirmation hypothesis was not evaluated.

Closeout preserves the hash-bound runtime byte-for-byte and records its stale
inherited descriptive fields in
`artifacts/post-outcome-runtime-metadata-discrepancies.md`. The prospective
confirmation-period correction is recorded before any confirmation access in
`artifacts/pre-confirmation-protocol-correction.md`.

## Artifacts

- `artifacts/design-freeze-v1.json` — prospective one-package contract.
- `artifacts/predecessor-manifest-v1.json` — exact accepted authorities.
- `artifacts/calibration-v1.json` — inherited candidate-blind thresholds.
- `artifacts/fits/` — fresh development fits and compact records.
- `artifacts/development-evaluation-v1.json` — staged model comparison.
- `artifacts/development-result-v1.json` — compact terminal and access record.
- `artifacts/candidate-freeze-v1.json` — produced only for one selectable
  candidate; correctly absent because none qualified.
- `artifacts/confirmation-*.json` — conditional sealed/consumed corpus,
  station fit, evaluation, and final decision; target-series and evaluation
  artifacts are correctly absent because confirmation was not reached.
- `artifacts/review.md` and `artifacts/gate-results.md` — accepted review and
  closure gates.
