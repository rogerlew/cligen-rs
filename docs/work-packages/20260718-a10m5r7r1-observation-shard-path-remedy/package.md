# A10M5R7R1 — Observation Shard Path Remedy

Status: `EXECUTED-HOLD-RESOURCE-AUTHORITY-EXHAUSTED`
Date: 2026-07-18
Evidence mode: Mixed
Starting branch and push target: `main` after A10M5R7 R0, push `main`

## Objective

Execute the unchanged A10M5R7 structural architecture identification after
correcting the observation-shard path and making its failure collection
satisfiable.

## Scope and invariants

This is an operational source remedy, not a new scientific design. It:

- changes the observation shard root from the nonexistent
  `corpus/artifacts/` directory to the corpus manifest's recorded
  candidate-fit Daymet path;
- publishes typed absence records if execution exits before any registered
  scientific product; and
- derives a new run from the existing resource authority without resetting its
  hash-chained ledger.

The A10M5R7 model identities, modes, groups, thresholds, decision order,
seeds, sites, horizons, temporal score, bootstrap, comparator identities,
protected-role firewall, and terminal vocabulary are unchanged. The original
failed attempt is not retried; R1 receives a fresh run identifier and source
commit under the same hash-chained ledger.

## Plan

1. Verify the six registered shard paths and identities directly against the
   immutable corpus archive before allocation.
2. Bind the corrected source commit and asset manifest into a derived run
   authority; plan, prepare, stage, verify, and submit one single-attempt L40
   job.
3. Collect and adjudicate the diagnostic products. Run the unchanged local
   temporal score only if the frozen generated-feedback gate advances.
4. Reconcile accounting, exact cleanup, roadmap/catalog, package review, and
   repository gates.

## Exit criteria

The scientific exits are exactly those in A10M5R7. Operational failure closes
at a named R1 hold with complete failure-shaped evidence and exact cleanup.

## Execution disposition

All six corrected shard paths and SHA-256 identities verified directly inside
the immutable corpus. The corrected source was committed and pushed at
`44e122f`, a revision-1 run authority was derived from R0's exact ledger head,
and `a10m5r7-architecture-r1` passed doctor, probe, plan, prepare, stage, and
verify without opening a protected role.

No R1 Slurm job was submitted. The ledger's conservative reservation rule
counts R0's settled primary at its full requested 60 minutes rather than its
three actual minutes, and also retains R0's five-minute recovery reservation.
The first R1 submission request (57 primary plus five recovery) failed closed
at `RESOURCE_CEILING`. A prospective amendment reduced the primary request to
52 minutes, but that request also failed because all 65 authorized minutes are
already reserved by the R0 lineage. No additional authority or budget was
created.

Toolkit abort could not collect a pre-submission success-shaped allowlist and
failed closed at `EVIDENCE_INCOMPLETE`. The exact staged R1 root was removed by
the owner-marker-validated toolkit cleanup script and verified absent. The
scientific diagnostic remains unexecuted. Its smallest blocker is explicit
operator authorization for a new package-scoped resource authority; creating
one autonomously would violate the toolkit's anti-budget-reset rule.

## Artifacts

- `artifacts/execution-disposition.md` — derived-run lifecycle and blocker;
- `artifacts/resource-ledger.md` — requested/actual accounting and refusal;
- `artifacts/gate-results.md` — local validation gates; and
- the unchanged A10M5R7 contract and jobs at the corrected source commit.
