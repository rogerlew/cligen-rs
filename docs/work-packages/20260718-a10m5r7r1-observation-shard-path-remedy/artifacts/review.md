# Package review

## Scope and science

PASS. The remedy changes only the observation-shard root and early-failure
publication. The model, weights, modes, random field, metric groups,
architecture decision, full temporal score, and protected-role firewall are
unchanged. No post-result threshold or selector was introduced because no
scientific result was produced.

## Evidence and operations

PASS with declared hold. Six source shard identities were checked before the
R1 allocation request. R0's traceback and authenticated gate were retained in
restricted state. Both durable roots and R0 job-local state are absent. R1 has
no scheduler identity and consumed no GPU time. The R0 collection and R1 abort
receipts remain failed-closed because their allowlists were not satisfiable;
the package does not relabel those failures as toolkit success.

## Decision

`HOLD-A10M5R7R1-NEW-RESOURCE-AUTHORITY-REQUIRED` is supported. Continuing on a
new authority changes external resource authorization and cannot be inferred
from the exhausted lineage. If authorized, the corrected, prospectively frozen
job is ready to execute without another scientific design decision.
