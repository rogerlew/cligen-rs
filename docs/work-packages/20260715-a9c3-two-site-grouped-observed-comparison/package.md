# A9c3 — Two-Site Grouped Observed-Development Comparison

Status: `EXECUTED-HOLD-NO-SELECTABLE-CANDIDATE`
Date: 2026-07-15
Evidence mode: Mixed
Scaffolding and execution authorization: operator dispatch on 2026-07-15
from clean `main` at `a0e24f0866f4536c168bfd809cb957d91e6d8bf3`,
targeting `main`

## Objective

Return the A9 successor-family campaign to the model question by replacing
only A9c's uncalibrated per-station hot-arid storm-descriptor floors with a
prospectively frozen equal-weight estimator over Yuma and Stovepipe Wells.
A9c3 refits both registered candidate classes under a fresh campaign identity,
compares them with faithful CLIGEN on the exposed six-regime development
surface, and freezes at most one research candidate for a separately
authorized A9d confirmation.

## Scope

Included:

- exact verification of the committed A9a, A9b, A9c, and A9c2 authorities;
- reuse of A9c's hash-bound Daymet and USCRN coefficient-fit/development
  objects without reacquisition or role changes;
- a pre-score grouped amendment that fixes Yuma and Stovepipe Wells, equal
  station weights, exact generated-side contributors, actual event
  frequencies, estimands, distances, uncertainty, and finite-estimator rules;
- candidate-blind grouped precision and power diagnostics before new fitting
  or scoring;
- fresh fits for all eight registered configurations of
  `alternating_renewal_marked_v1` and `latent_regime_marked_v1`;
- staged 30- and 100-year development comparison with faithful CLIGEN,
  site/stratum/family diagnostics, deterministic replay, a complete
  31-objective availability vector, and the frozen A9 selection rule;
- at most one sealed research candidate, a public scientific report,
  consolidated review, and one registered terminal.

Excluded:

- changing any A9c or A9c2 result, count, threshold, or terminal;
- adding or substituting development stations or applying arbitrary event-
  count support floors;
- touching any locked A9a confirmation station-year series;
- changing faithful generator code, production Rust profiles, station
  schemas, runtime classification, fallback, or realized-output repair;
- executing A9d, A9e, openWEPP, or WEPPcloud integration.

## Authority

- [A9c2 operator disposition](../20260715-a9c2-grouped-hot-arid-reentry/artifacts/post-acceptance-operator-disposition.md)
  accepts the two sites and defines the successor obligations.
- [A9a objective registry](../20260715-a9a-successor-family-foundation/artifacts/objective-registry-v1.json)
  remains authoritative except for the explicit package-local grouped storm-
  descriptor amendment.
- A9c's observed-role/source manifests, candidate implementation, candidate-
  blind non-storm thresholds, and role firewall remain authoritative and are
  hash-bound in `artifacts/predecessor-manifest-v1.json`.
- ADR-0001 protects faithful generation. ADR-0002 and ADR-0004 continue to
  govern quality adjudication and any later production promotion.

## Plan

1. Verify predecessor identities, source commit, clean dispatch, role
   firewall, and zero confirmation-series access.
2. Freeze the new campaign, grouped estimands, equal weights, actual-frequency
   rule, generated contributors, bootstrap identities, stage-promotion rules,
   thresholds, fit-identifiability rules, and terminal rule before any A9c3
   candidate output exists.
3. Bootstrap the two accepted hot-arid event series without candidate input;
   publish finite-estimator, interval, precision, and power diagnostics.
4. Refit every registered configuration of both actual model classes and
   reproduce structural recovery/non-isomorphism evidence.
5. Generate a faithful comparator and execute short screen, full development,
   and eight-burn replay at nested 30/100-year horizons.
6. Publish every objective's availability and distance, station/stratum/
   family summaries, attempt inventory, Pareto trace, and lexicographic
   selection result.
7. Seal at most one candidate or record the named hold; author and review the
   report, run all gates, and close the package and roadmap.

## Execution & dispatch

The operator authorized scaffolding and execution on 2026-07-15 from clean
`main` equal to `origin/main` at
`a0e24f0866f4536c168bfd809cb957d91e6d8bf3`. `main` is the only push target.
This dispatch does not itself authorize committing or pushing the A9c3 result.

## Gates

- predecessor hashes, role paths, normalized hashes, and A9b replay pass;
- the design freeze predates grouped calibration, new fits, and candidate
  scores;
- grouped calibration uses no candidate input and every registered estimator
  is finite;
- both candidate classes have complete fresh fits and pass structural audits;
- inherited monthly reconciliation tolerances pass for every retained fit;
- deterministic replay, calendar/support, provenance, and 30-year-prefix
  checks pass;
- all 31 objectives publish status; mandatory availability is not converted
  to a favorable zero;
- selection bytes reproduce and no more than one candidate is sealed;
- confirmation target series remain unaccessed;
- the scientific report and review have zero open P1/P2 findings;
- `git diff --check` plus an untracked authored-text whitespace scan;
- `cargo fmt --check`;
- `cargo clippy --all-targets -- -D warnings`; and
- `cargo test`.

Coverage/CRAP is not triggered unless production functions under `crates/`
change; such changes are outside this package.

## Exit criteria

Success is exactly `CANDIDATE-FROZEN-READY-A9D` with one complete research
freeze. Legitimate holds are `HOLD-A9C3-PREDECESSOR-INTEGRITY`,
`HOLD-A9C3-DATA-ROLE`, `HOLD-A9C3-GROUP-ESTIMATOR`,
`HOLD-A9C3-MODEL-CLASS-EQUIVALENCE`, `HOLD-A9C3-FIT-APPLICABILITY`,
`HOLD-A9C3-MONTHLY-RECONCILIATION`,
`HOLD-A9C3-NO-SELECTABLE-CANDIDATE`, or `HOLD-A9C3-RESOURCE-BOUND`.
Every terminal preserves the evidence and authorizes neither confirmation nor
runtime work by itself.

## Artifacts

- `artifacts/design-freeze-v1.json` — pre-outcome campaign and grouped rule.
- `artifacts/grouped-calibration-v1.json` — candidate-blind two-site
  uncertainty and power diagnostics.
- `artifacts/fits/` — fresh immutable research fits.
- `artifacts/evaluation-v1.json` — canonical objective, stage, and selection
  evidence.
- `artifacts/review.md` — consolidated report/package review.
- `artifacts/gate-results.md` — exact closure gates and terminal.
