# Scaffold gates

- exact R14R1 source and prepared-manifest identity
- publication-time authenticated R14R1 ENOSPC terminal evidence
- byte-identical R14 science/objective/temporal/calendar/selector assets
- byte-identical inherited R14 core plus adapter-only/explicit-total interface repair
- caller/callee parameter composition totals 278667/278747/279819/279899
- exact two-role plan: one-L40 control, four-L40 portfolio
- 30 + (4 x 240) + 5 = 995 GPU-minute-equivalent ceiling
- one attempt per primary role; no retry, array, DDP, NCCL, or `torchrun`
- exact four-candidate-to-device mapping 0..3
- exactly four unique allocated NVIDIA L40 devices
- each child sees exactly one GPU and reports the expected physical identity
- one authenticated bootstrap and one shared corpus extraction
- immutable shared environment/corpus before child launch
- disjoint durable output and job-local cache roots
- any child failure is retained; launcher waits/reaps all children then fails the portfolio
- scheduler signals terminate the complete supervised process tree
- all four evidence role directories retained on every terminal path
- exact supervised job-local cleanup or authenticated recovery target
- calendar/missingness preflight before reservation
- protected and solar roles sealed
- package tests, shell syntax, `git diff --check`, and repository gates

Coverage/CRAP is not triggered because no production function under `crates/`
changes.
