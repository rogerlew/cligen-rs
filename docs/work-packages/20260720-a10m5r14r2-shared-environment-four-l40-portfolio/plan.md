# Execution plan

1. Close R14R1 honestly, settle every submitted job, collect its retained
   evidence, clean exact remote and job-local roots, and publish an
   authenticated ENOSPC terminal receipt in this package.
2. Authenticate the exact R14R1 prepared asset manifest and source commit.
   Prove byte identity for every frozen R14 science, objective, temporal,
   calendar, selector, role, seed, and evidence-profile asset.
3. Publish R14R2 to `main`. Prepare a fresh asset tree by removing inherited
   admission receipts and overlaying only the new operational capacity
   contract, authority builder, admission materializer, and portfolio wrapper.
4. Build fresh R14R2 authority, budget, run, plan, and admission identities.
   Complete the calendar/missingness preflight before reservation.
5. Materialize and authenticate the control admission, submit the 30-minute
   one-L40 control, and require its validated pass.
6. Materialize and authenticate the single portfolio admission. Require an
   idle qualified four-L40 single-node allocation, then submit one 240-minute
   job requesting `gpu:l40:4`.
7. In the portfolio job, authenticate four unique allocated L40s, bootstrap one
   shared canonical environment, extract one shared corpus, delete wheel and
   pip payloads, make shared trees read-only, and launch four independent
   non-DDP processes with the frozen CUDA/device map and isolated caches.
8. Wait for all science children, then fail the portfolio if any child or
   isolation gate failed. Preserve all four
   role directories, the portfolio setup/device/process receipts, and Slurm
   evidence regardless of success or failure.
9. Observe, pack, and collect under the unchanged 256 MB/64 MB evidence
   contract. Preserve streams and checkpoints until authenticated replay.
10. Authenticate collection, plan, source, allowlist, candidate identities,
    and all 188 selector metrics. Replay twice and require byte identity before
    cleanup.
11. Record the scientific disposition, clean exact remote and job-local state,
    release unused recovery reserve, close the toolkit run, and reconcile the
    roadmap, catalog, and campaign ExecPlan.

No authority creation or HPC submission occurs during scaffolding.
