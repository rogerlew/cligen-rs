# A10M5R4R2 prospective operational amendments

## Revision 1 — satisfiable failure collection

Before planning, staging, allocation, PRISM access, or generated-output access,
the wrapper was amended to publish a typed `streams.json` absence record when
a model role exits before stream publication. The scientific contract, sites,
models, metrics, thresholds, roles, job count, time limits, and resource
ceiling are unchanged. This makes the evidence allowlist satisfiable on both
success and the prospectively legitimate exact-export-reproduction failure.

The initialized resource authority is continued with `derive-run`; it is not
reset. No resource was reserved or consumed under revision 0.
