# Test results

Prospective validation in progress.

- Bundled Python 3.12.13 / NumPy 2.3.5 evaluator tests: passing.
- Strict manifest validation: passing.
- `cargo fmt --check`: passing.
- `cargo clippy --all-targets -- -D warnings`: passing.
- `cargo test`: passing.

Realized execution, replay, and terminal gates remain pending publication of
the prospective source commit.

First-source attempt `b34e94104208df868bd602dea21f1b9723850362`
failed safely before terminal publication on an inherited selector-component
tolerance. The amendment diagnostic measured maximum distance drift
`6.68e-13 km`; no ranks, scores, pools, or winners differed.
The authenticated diagnostic also records four production repair failures.
The formal amended run is expected to exercise the frozen
`HOLD-REPAIR-INELIGIBLE` path after completing all 2,400 feasibility cells;
quality scoring must remain false.
