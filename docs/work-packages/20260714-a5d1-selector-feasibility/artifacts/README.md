# A5d1 Artifact Inventory

Status: `EXECUTED-HOLD-PATH-INFEASIBILITY`

## Controlling v6 record

- `selector-feasibility-contract-v4.json` and schema — final amended
  development feasibility contract, including aggregate January transition
  ownership.
- `evidence-lock-inputs-v6.json`, `exposure-ledger-v6.md`, and
  `pre-solver-freeze-v6.json` — zero-confirmation input lock and immutable
  pre-v6-outcome identity.
- `pre-solver-freeze-amendment-005.json` — prospective v6 correction and v5
  disposition boundary.
- `development-library-manifest-v1.json` and
  `year-feature-manifest-v1.json` — regenerated 17-station faithful-off
  library and feature identities.
- `marginal-results-v1.json`, `path-results-v1.json`, and
  `selector-feasibility-results-v1.json` — published 34-cell marginal,
  306-cell path, and cross-station aggregates.
- `detailed-evidence-v1.tar.gz` and manifest — deterministic archive of 34
  marginal certificates and 306 detailed paths. The archive is Git
  LFS-managed; the JSON aggregates and manifest remain ordinary Git text.
- `physical-row-identity-audit-v1.json` — independent 306-cell physical suffix
  and common-prefix audit.
- `a5d1-decision-v1.json` and `feasibility-report.md` — machine decision and
  reviewed scientific account.
- `review.md`, `gate-results.md`, and `closure-evidence-v1.json` — review,
  replay/gate record, and immutable terminal closure manifest.

The reproducible 256-year daily libraries, feature files, and live certificate
and path workspace remain under ignored `target/a5d1-selector-feasibility/`.
They are about 193 MiB and are not repository artifacts. The compact retained
archive is the checkout-auditable detailed evidence snapshot.

## Execution tools and fixtures

`a5d1_common.py`, the library/feature builders, solver, path runner,
finalizer, deterministic archiver, synthetic fixture runner, physical audit,
and strict package verifier are hash-bound by the v6 lock/freeze. The 15
prospective fixtures cover feasible and infeasible weights, tolerance edges,
calendar/reuse/iteration exhaustion, certificate tampering, centered
linearization, zero-weight selection, directional boundaries, prefix render,
and aggregate January transition cases.

## Superseded and invalidated development records

Earlier records are retained to preserve outcome-access chronology; none is
terminal evidence.

- V1 was superseded before solver/path outcomes when whole-file repeat
  comparison included an output pathname.
- V2 was invalidated for an incorrect uniform-reference denominator.
- V3 was invalidated after independent review found centered-target,
  detrending, finite-prefix, boundary, and render defects.
- V4 was invalidated because fitted monthly replay omitted realized January 1
  transitions; its completed outputs and archive use `invalidated-v4-*` names.
- V5 was stopped after 65/306 path records because its boundary-subset fitted
  gate was overrestrictive; it issued no complete matrix or decision. Its
  available manifests/marginal aggregate use `invalidated-v5-*` names.

The numbered contracts, locks, freezes, exposure ledgers, amendments, and
`invalidated-v2-*` through `invalidated-v5-*` artifacts document those stages.
They must not be substituted for the v6 identities above.

## Public-safety boundary

This tree contains only exposed development-station evidence and synthetic
fixtures. It contains no confirmation object or target, confirmation WEPP
response, production behavior change, public candidate/profile identifier, or
change to an accepted specification.
