# A10M5R15R2R3 — Portfolio Evidence Remedy

Status: `EXECUTION-READY`
Date: 2026-07-22
Starting branch and push target: current `main`, push `main`

## Objective

Re-execute the ratified SPEC-A10-EXTERNAL-NORMAL-CONDITIONING revision 2 study
without changing its scientific contract, while correcting the portfolio
evidence allowlist so every new-role base diagnostic is retained. The remedy
must make any launcher or child failure diagnosable and must not weaken a gate.

## Frozen execution

- run id: `a10m5r15r2r3-portfolio-evidence-remedy-r0`;
- one 30-minute control followed by one two-L40, two-wave portfolio;
- wave 0: E0/E1; wave 1: E2C/E2;
- three seeds per arm: 147031, 271828, 314159;
- maximum 515 L40-minute-equivalent, including five recovery minutes;
- exact A10M5R15R1 98-object corpus and conditioning identities;
- exact R14R2R2 r3 launcher/control/replay lineage where unchanged;
- exact six-row successor-corpus control identity calibrated by A10M5R15R2R1;
- two fixed-horizon portable CPU exports per fitted seed, with the normative
  GPU-hidden one-core benchmark executed in the portfolio allocation after all
  trainer processes exit;
- no confirmation, solar, spatial, or production roles.

## Model identities

- E0 reconstructs R14 arm B exactly on the successor corpus and excludes
  normals from its mapping.
- E1 extends E0's four location-head climatology mapping from 20 to 200
  bias-free inputs; its 180 normals-only input columns (720 weights) begin at
  exact zero.
- E2C replaces P2 with one bias-free 20-to-15 descriptor baseline plus the
  inherited centered 1,740-parameter medium/slow OU adapter.
- E2 shares E2C's descriptor initialization, keeps 11 non-location heads on
  the 20-term mapping, and maps four location heads from 200 terms. Its
  normals-only weights begin at exact zero.

The training objective, optimizer, checkpoint operator, member roster,
continuous-time discretization, supports, wet-amount family, temporal protocol,
and paired random fields are those ratified in the parent science contract.

## Gates

Source publication precedes authority. Preparation authenticates the exact
R14R2R2 r3 assets, R1 corpus/conditioning receipts, and every transfer object.
Local fixtures must prove parameter counts, matched initialization, normals
exclusion, candidate-fit-only normalization identity, no-reset OU behavior,
structural centering, role topology, resource arithmetic, and protected-role
sealing. Fresh toolkit authority, control, occupancy, and admission checks
remain mandatory despite the operator's GPU authorization.

Candidate science runs once. Operational retry is limited to the recorded
five-minute recovery allowance. Two authenticated selector replays and the
normative CPU benchmarks precede one terminal disposition.
Candidate safeguard failures reject only the affected arm and its matched
treatment pair. Faithful-comparator nondeterminism or incompleteness remains a
global benchmark-integrity failure. A fresh independent R2R3
execution-readiness review is mandatory; its findings and dispositions are
package-local.

## Successor correction and authorization

R2R2 control job 1060866 completed all six successor identities exactly.
Portfolio job 1060868 then failed after both wave-0 children exited 1, but its
inherited allowlist still named predecessor role directories and therefore did
not collect the new roles' `candidate.stderr` files. R2R3 changes only that
operational evidence surface: it deterministically rewrites every inherited
role-scoped evidence path to the four ratified R2 role names before adding the
portable export paths. Checkpoint, cursor, validation, model, temporal,
attribution, resource, and selection contracts remain unchanged.

The operator authorized the 515-minute study within a 597-minute outer
campaign ceiling. Prior realized use is 60 L40-minutes, so the bounded maximum
after this study is 575. Authority, assets, admission, run root, and recovery
reserve are fresh; no failed-run execution identity is reused.

## Execution state

Independent readiness review closed `PASS` after two P2 findings were
dispositioned with an executable 64-path rebinding fixture and reconciliation
of the living ExecPlan. Publication and execution remain pending. Candidate
output and protected confirmation roles remain unopened.
