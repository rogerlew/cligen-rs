# A10M5R15R2R3 Disposition

Status: `HOLD-A10M5R15R2R3-E2-INITIALIZATION-IMPLEMENTATION`

The evidence-surface remedy is validated: it retained the exact child
diagnostic that R2R2 lost. The study itself remains uninterpretable because
both wave-0 processes stopped in the shared startup self-test before training.

The exception identifies a bounded implementation error. PyTorch advanced
indexing returns a temporary tensor, so calling `.copy_()` on
`location_baseline.weight[:, descriptor_columns]` does not update the module
weight. E2 therefore did not receive E2C's four location-head descriptor
initialization, and the invariant correctly failed. A fresh successor may
replace that operation with an actual indexed assignment or `index_copy_`, pin
the behavioral fixture, and otherwise preserve the scientific contract.

Control and portfolio charges were 19 and three L40-minutes. With 60 prior
minutes, realized use is 82. The unused recovery reserve was released after
authenticated cleanup; 82 + 515 equals the authorized outer ceiling of 597.
