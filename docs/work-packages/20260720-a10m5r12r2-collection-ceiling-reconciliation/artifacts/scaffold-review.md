# Scaffold review

Disposition: `ACCEPT FOR PUBLICATION AND EXECUTION`

Independent review held the first drafts for an undefined live source path,
unauthenticated replay-triggered cleanup, missing gate-receipt authentication,
an in-place canonical-profile mutation, absence of an independent post-clean
probe, an unsafe identity-file override, incomplete parent plan/ledger binding,
and a non-restartable destructive boundary. Each hold was resolved before
publication.

The accepted design now authenticates the exact parent archive, authority,
linked ledger and stranded five-minute recovery reservation, state, raw and
semantic plan relation, three job/gate receipts, and published source. The
separately published replay runner binds the recovered 51-file projection,
binary, corpus, Daymet shards, runtime, PRISM data-tree identity, all selector
sources, predecessor provenance, two byte-identical outputs, allowed terminal,
and protected-role seal. Cleanup holds the parent run lock, writes an immutable
intent before deletion, revalidates the exact remote archive and marker, uses
the committed marker-bound cleaner, and independently proves the remote root
absent. An interrupted cleanup resumes only from the exact authenticated
intent.

The canonical `lemhi-v2.json` profile and its designated configuration remain
unchanged. Prospective large-stream packages select the separate
`lemhi-v2-large-evidence.json` profile and declare plan-specific evidence
ceilings.

Review evidence: 11/11 R2 fixture tests and 84/84 toolkit tests pass; live
read-only parent validation returns `MATRIX_SETTLED` with 51 exact members.
R2 authorizes zero allocation. The parent ledger remains byte/head unchanged:
99 GPU-minutes settled, five reserved/stranded, 104 committed of 395.
