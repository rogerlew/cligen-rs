# Execution disposition

Terminal: `A10M5R10-PORTFOLIO-READY`

The complete frozen portfolio ran under one coherent R1R4 authority. Control
job `1014057` reconstructed all six matched P1/P2 controls. Ten independent
candidate jobs then completed in five ordered two-role waves, publishing all
30 seed rows with all execution, calendar, identity, physical-support,
gradient-free evaluation, and cleanup gates true. The R1R4 parent-environment
remedy therefore closed the last operational prerequisite without changing the
scientific comparison.

Four configurations were eligible and nondominated. The frozen selector
retained, in order:

1. `annual_monthly_residual_adapter-k1`;
2. `monthly_residual_adapter-k2`; and
3. `annual_monthly_residual_adapter-k2`.

The annual/monthly residual family is supported at both tested capacities and
the monthly-only family remains useful at K2. The K1 annual/monthly adapter is
the parsimonious retained member: its 87,370 parameters were within the frozen
2% equivalence band on the first retention axis. K2 annual/monthly delivered
the strongest retained median annual-dispersion ratio (`0.7641`) and a median
monthly-dispersion ratio of `0.9601`. Monthly K2 was effectively equivalent on
the dispersion retention axis and won its predeclared parameter tie-break.
All three preserve monthly and annual location, wet/dependence structure,
within-month daily dispersion, and proper NLL within the frozen guards.

`hierarchical_joint_factor_adapter-k2` was also eligible and nondominated. It
was not rejected scientifically; after the other two selections it fell within
the daily-NLL equivalence band and lost the predeclared parameter-count
tie-break to annual/monthly K2. It remains fully represented in the comparison
record but is not part of the bounded retained interface.

The climate-normal state-space family failed the daily-NLL and combined-
dispersion gates at both capacities. The physics-conditioned family improved
its solar dependence and family aggregates, but its solar location and
interannual-dispersion blocks degraded far beyond the 10% per-block guard; it
also failed the core combined-dispersion gate. This result supports the earlier
decision to establish the smaller core stochastic adapter before adding broad
solar/latitude/day-of-year/precipitation structure. The physics relationship is
not ready for retention in its tested form.

The selector self-test passed and a second raw-evidence replay before cleanup
reproduced all four selector files byte for byte. The toolkit independently
authenticated a 6,952,960-byte archive containing all 153 allowlisted evidence
files and no absences. Publication projection redacted private path-bearing
metadata as designed; exact selector replay remained against the authenticated
raw run root so its internal control-summary digests were unchanged.

All 11 jobs settled at 396 actual GPU-minutes against the 935-minute ceiling.
No recovery job ran, its five-minute reserve was released, every job-local root
was absent, the exact durable run root was removed, and the toolkit closed
normally. No protected development-confirmation role was opened. This package
identifies a development portfolio; temporal, spatial, confirmation, public-
profile, and production claims remain outside its authority.

The next defensible science package is a prospective temporal-generalization
comparison of the three retained configurations under one matched protocol.
That is a new scientific/resource authority, not follow-through authorized by
this package, and confirmation data must remain sealed until its own entry
conditions and firewall are frozen.
