# Execution disposition

Disposition: `PRISM-MODE-BUNDLE-PEDIGREE-READY`

The Cargo-distributed stochastic PRISM-localized comparator is complete as a
self-describing public comparison mode. Its runtime normals and exact source
are immutable release assets pinned by byte count and SHA-256; acquisition is
an explicit `cligen prism sync`, while `query` and `run` operate locally.

Every successful run now emits a strict `method.json` and binds it into the
artifact manifest. The record separates the FSWEPP/Rock:Clime concept and
station origin, the reviewed WEPPcloud/`wepppy` implementation lineage, and
the independently specified cligen-rs behavior. It also carries the nine
registered limitations, including monthly-normal forcing, inherited station
structure, heuristic occurrence/intensity changes, point-cell treatment,
static climatology, lack of multi-point coherence, and comparator-only claim.

The profile remains `stochastic_prism_localized_par_v1`. Exact before/after
checks establish that the package changed provenance output only: the climate,
localized station, PRISM receipt, selector receipt, and localization receipt
are byte-identical. The method record must not be read as a general quality
certification or as behavioral identity with either earlier lineage stage.

This package closes the bundle and public-mode documentation independently of
the stopped A10 neural-capacity sequence. It does not reopen A10M5R5 or later
promotion stages.
