# A10M5R11R1 — Admission Role-Matrix Remedy

Status: `SCAFFOLDED`
Date: 2026-07-19
Evidence mode: Mixed
Starting branch and push target: current `main`, push `main`

## Objective

Execute the unchanged A10M5R11 retained-adapter temporal-generalization
science after correcting the inherited pre-submit checker to authenticate the
contract-derived four-role matrix instead of requiring the predecessor's
literal 11-role portfolio.

## Predecessor and bounded correction

A10M5R11 passed source, asset, login, stage, and remote verification, then
failed `exact_role_matrix` before submission. Zero jobs and zero GPU-minutes
were opened; exact pre-submission abort removed the durable root. This package
binds that committed hold.

The only operational changes are:

- require `len(unique plan roles) == len(contract roles) + 1` and require that
  value to equal `1 + resources.candidate_role_count`;
- make close-on-any-observed-candidate-failure explicit in the contract; and
- use fresh package, run, authority, budget, admission-record, and job-local
  identities.

The three candidates, controls, training objective, seeds, six sites,
100-year/eight-member streams, calendar, metrics, scales, comparators,
bootstrap, eligibility, concurrency, attempts, and 305 GPU-minute ceiling are
unchanged. Confirmation and solar remain sealed.

## Execution sequence

Run the exact control predecessor. After it passes, run annual/monthly K1 and
monthly K2 as the first two-role wave, one bootstrap at a time, then run
annual/monthly K2. Collect authenticated streams, generate comparator arms
locally, replay the selector twice, settle resources, remove exact remote and
job-local state, and review the complete record.

## Exit criteria

The scientific terminals remain `A10M5R11-TEMPORAL-READY` and
`HOLD-A10M5R11-NO-TEMPORALLY-ELIGIBLE-CANDIDATE`. An identity, admission,
execution, evidence, resource, or cleanup failure receives an exact
`HOLD-A10M5R11R1-*` terminal and no scientific interpretation.
