# Scaffold review

Disposition: `ACCEPT`
Date: 2026-07-19

The predecessor hold, zero-resource state, and exact cleanup are committed and
bound. The role gate now derives both uniqueness and count from the plan and
contract; changing either roster or `candidate_role_count` fails closed. The
failure-closure boolean required by the same checker is explicit.

Normalized science files differ only where package/run/admission identities
are operationally required. Candidates, controls, objective, temporal
contract, sites, metrics, comparator protocol, selector, resources, and solar/
confirmation boundary are unchanged. Accepted for fresh authority.
