# A10M5R3 operational chronology

1. `r1` failed during staging because nested recovery/log placeholder parents
   were absent. It aborted before submission and proved its remote root absent.
2. `r2` job 1013955 completed its scientific row, but a generated wrapper
   escape error prevented receipt promotion. It settled failed after 274
   seconds; the exact marked root was cleaned and evidence retained privately.
3. `r3` jobs 1013956--1013964 completed the nine family rows. Job 1013965
   failed in two seconds because `resolve.py` ran under Lemhi's older ambient
   Python before canonical CPython 3.11 was created. The exact root was cleaned.
4. `r4` used corrected source `47963a9`; jobs 1013966--1013988 (with Slurm's
   normal noncontiguous IDs) completed all 18 frozen roles and all gates.
5. Raw collection authenticated 239 allowlisted files, then projection failed
   because `[REMOTE_RUN_ROOT]` did not match the projection-v3 reserved-token
   grammar. The settled plan was not amended.
6. The prospective A10M5R3R1 successor validated that replacing only the token
   spelling with `<REMOTE_RUN_ROOT>` projected every authenticated raw file
   under the unchanged sanitizer and forbidden-value scan. It published the
   recovered evidence and performed marker-bound exact cleanup.

The first two wrapper defects and the token grammar should have been caught by
preflight validation. More importantly, r2--r4 should have remained revisions
under the original authority and resource budget. Canonical guidance now calls
out all three traps explicitly.
