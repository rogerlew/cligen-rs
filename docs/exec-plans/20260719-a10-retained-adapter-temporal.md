# A10 retained-adapter temporal generalization ExecPlan

## Purpose

Determine which A10M5R10 retained residual adapters reproduce stochastic
monthly and yearly climate across the ratified six-regime temporal protocol.

## Progress

- [x] Audited the retained portfolio and ratified temporal protocol.
- [x] Froze the three-candidate scope and downstream solar boundary.
- [ ] Publish the scaffold and immutable execution assets.
- [ ] Execute the control and three candidate roles on typed L40s.
- [ ] Collect, replay, disposition, review, and reconcile the package.

## Surprises

None at scaffold. The protocol already contains the required monthly and
annual dispersions and an explicit leap-century-safe bootstrap year axis.

## Decision Log

- 2026-07-19: inherit A10M5R4R2 temporal metrics, comparators, scales,
  bootstrap, and noninferiority gates without modification.
- 2026-07-19: retain all passing configurations; do not add another
  parsimony selector.
- 2026-07-19: reserve solar for a later partially procedural latitude/day-of-
  year envelope plus stochastic weather-coupled residual.

## Outcomes

Pending execution.

## Context

A10M5R10R1R4 retained annual/monthly K1, monthly K2, and annual/monthly K2
after a matched architecture portfolio. That result used training-objective
evidence. A10M5R11 supplies the missing realized 100-year temporal
generalization adjudication against observed Daymet and the faithful/PRISM
reference.

## Plan of Work

Materialize the six exact P1/P2 frozen controls once. Run one independent job
per retained configuration, with all three training seeds inside each job.
Each job produces the complete six-site, eight-member, 100-year stream
matrix. Execute at most two primary jobs concurrently. Collect only
allowlisted evidence, regenerate comparator arms locally, run the selector
twice, and close only after byte-identical replay and exact cleanup.

## Concrete Steps

1. Verify predecessor, source, science, corpus, calendar, and toolkit identity.
2. Publish the package scaffold to `main`; bind that commit in fresh authority.
3. Stage immutable assets and run the control predecessor.
4. Run two candidate roles concurrently, then the third.
5. Collect authenticated evidence and settle actual GPU minutes.
6. Generate faithful and localized stochastic-PRISM comparator arms locally.
7. Run the leap-safe selector twice and compare bytes.
8. Write disposition, review, catalog, roadmap, ledger, and cleanup records.

## Validation

The package requires its freeze tests, shell syntax, JSON parse, Python
compilation/tests, Cargo formatting, Clippy, and tests. Execution additionally
requires complete support-valid streams, exact evidence closure, selector
replay identity, and durable/job-local cleanup.

## Idempotence and Recovery

Source and local evaluation are replayable. Remote primary jobs are one
attempt per role and are never spliced. A five-GPU-minute reserve permits only
exact-node cleanup recovery. Any scientific rerun receives a new package and
authority.

## Artifacts

The work package owns its contract, site roster, science identities, staged
job sources, authenticated toolkit records, temporal results, disposition,
review, and gate log. Private authority and scheduler credentials remain
outside Git.

## Interfaces

`SPEC-A10-RETAINED-ADAPTER-TEMPORAL` defines the stream matrix, calendar
hardening, inherited selection rule, and solar boundary.
